# v0.1.8 Params-Flow Live Smoke Evidence Reference

Date: 2026-05-24

Scope: durable reference for the 0.1.8 `dashboard-params-flow` live Agent
smoke evidence before 0.1.9 replaces `test-results/agent-smoke/latest/` with
`dashboard-invalid-param` evidence.

Status: preserved for audit reference

## Raw Evidence Location

The 0.1.8 params-flow raw evidence remains available through commit
`c6da552`, where `test-results/agent-smoke/latest/` pointed to the validated
`dashboard-params-flow` run.

Before 0.1.9 replacement, the latest raw directory contained:

- `result.json`
- `operation-log.jsonl`
- `transcript.md`
- `console.log`
- `api-baseline.json`
- `api-summary.json`
- `screenshots/dashboard-params-flow.png`

## Recorded Result

Scenario: `dashboard-params-flow`

Status: `pass`

Verdict source: `deterministic_checker`

Validated command:

```bash
make validate-agent-smoke-result RESULT_DIR=test-results/agent-smoke/latest
```

The recorded `result.json.commands` contained these successful command records:

```text
tools/testing/agent_smoke_evidence.py baseline --base-url http://127.0.0.1:8000 --out test-results/agent-smoke/latest/api-baseline.json
tools/testing/agent_smoke_evidence.py collect --scenario dashboard-params-flow --base-url http://127.0.0.1:8000 --baseline test-results/agent-smoke/latest/api-baseline.json --out test-results/agent-smoke/latest/api-summary.json --operation-log test-results/agent-smoke/latest/operation-log.jsonl
make validate-agent-smoke-result RESULT_DIR=test-results/agent-smoke/latest
```

Each recorded command had `exit_code: 0`.

## Deterministic Evidence Summary

The helper-generated `api-summary.json` recorded:

```json
{
  "scenario": "dashboard-params-flow",
  "health_status": "ok",
  "param_path": "counter.increment",
  "expected_value": 2,
  "observed_value": 2,
  "before_tick": 0,
  "after_tick": 1,
  "counter_event_tick": 1,
  "counter_event_increment": 2
}
```

## Operation Boundary

The operation log recorded UI and CLI operations only:

- helper baseline CLI evidence collection.
- dashboard open.
- params path/type/value UI input.
- params apply UI click.
- params JSON UI observation.
- runtime step UI click.
- helper collect CLI evidence generation.
- deterministic validator CLI command.

No direct API operation was recorded as an Agent operation.

## 0.1.9 Replacement Note

0.1.9 may replace `test-results/agent-smoke/latest/` with validated
`dashboard-invalid-param` evidence. After replacement, the current raw
`latest/` directory should be interpreted as invalid-param evidence; the
params-flow live smoke status remains durable through this summary and commit
`c6da552`.
