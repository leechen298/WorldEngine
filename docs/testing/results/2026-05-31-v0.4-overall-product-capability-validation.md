# v0.4 Overall Product Capability Validation

Status: clean pass after repair rerun
Date: 2026-05-31

## Scope

This run validates the current v0.4 product surface after post-closeout test
expansion:

- backend Agent Loop, params, runtime, event, archive, and API behavior.
- dashboard UI flows and browser E2E behavior.
- Agent UI/CLI smoke evidence with deterministic checker verdict.
- minimal autonomous saved-result evidence with scorecard checker verdict.
- documentation and evidence consistency for current testing status.

The original package `02-overall-product-capability-validation` did not repair
product code and ended partial pass. Follow-up repair package
`03-frontend-build-type-repair` repaired only the frontend TypeScript build
failure and reran the clean-pass validation matrix.

## Original Validation Commands

| Command | Exit | Result |
| --- | ---: | --- |
| `make check-backend` | 0 | backend venv present |
| `make check-frontend` | 0 | frontend dependencies present |
| `cd backend && .venv/bin/python -m pytest app/tests/test_agent_perception.py app/tests/test_agent_action_adapter.py app/tests/test_agent_loop_service.py app/tests/test_agent_loop_api.py -q` | 0 | 24 passed in 0.39s |
| `cd backend && .venv/bin/python -m pytest app/tests tests -q` | 0 | 139 passed in 1.57s |
| `cd frontend && pnpm test` | 0 | 6 files passed, 28 tests passed |
| `cd frontend && pnpm build` | 1 | TypeScript build failed |
| `cd frontend && pnpm exec playwright test e2e/agent-loop.spec.ts` | 0 | 9 passed in 1.3s after reviewer-driven assertion hardening |
| `make test-e2e` | 0 | 15 passed in 6.5s after reviewer-driven assertion hardening |
| `make validate-agent-smoke-fixtures` | 0 | fixtures passed, expected invalid fixture failed, 25 tests passed |
| `make validate-agent-smoke-result RESULT_DIR=test-results/agent-smoke/latest` | 0 | PASS |
| `make validate-agent-autonomous-fixtures` | 0 | fixtures passed, six expected invalid fixtures failed, 9 tests passed |
| `make validate-agent-autonomous-result RESULT_DIR=test-results/agent-autonomous/20260531T122230+0800` | 0 | PASS |
| `git diff --check` | 0 | no whitespace errors |

## Repair Rerun Commands

| Command | Exit | Result |
| --- | ---: | --- |
| `cd frontend && pnpm build` | 0 | `vue-tsc -b` and Vite production build passed; 3170 modules transformed; Vite chunk-size warning only |
| `pnpm --dir frontend exec vue-tsc -b --pretty false` | 0 | focused reviewer type verification passed |
| `cd frontend && pnpm test` | 0 | 6 files passed, 28 tests passed |
| `make test-e2e` | 2 | sandbox run could not bind `127.0.0.1:8000`; approved rerun required |
| `make test-e2e` | 0 | 15 passed in 6.6s after approved rerun outside sandbox |
| `make validate-agent-smoke-result RESULT_DIR=test-results/agent-smoke/latest` | 0 | PASS |
| `make validate-agent-autonomous-result RESULT_DIR=test-results/agent-autonomous/20260531T122230+0800` | 0 | PASS |
| `git diff --check` | 0 | no whitespace errors |

## E2E Result

Passed.

Full E2E executed 15 Playwright tests:

- 9 `agent-loop.spec.ts` tests covering bounded perception, params patch,
  rejected action, noop/patch rejection, empty patch rejection, dry-run
  rejection metrics, multi-patch/remove, schema errors, and `event_limit`
  boundaries.
- 6 `dashboard.spec.ts` tests covering runtime, params, invalid params,
  params-agent Auto-Tune, timeline navigation, and archive summary rendering.

E2E report artifact:

- `test-results/e2e/html-report/index.html`

The final Playwright run retained no per-test failure artifact directory.

## Agent Test Result

Agent smoke passed.

- Verdict source: `deterministic_checker`.
- Result directory: `test-results/agent-smoke/latest`.
- Key artifacts:
  - `result.json`
  - `operation-log.jsonl`
  - `api-summary.json`
  - `screenshots/dashboard-agent-autotune.png`

Minimal autonomous saved-result validation passed.

- Verdict source: `scorecard_checker`.
- Result directory: `test-results/agent-autonomous/20260531T122230+0800`.
- Key artifacts:
  - `result.json`
  - `operation-log.jsonl`
  - `scorecard-summary.json`
  - `transcript.md`
  - `console.log`
  - `screenshots/dashboard-agent-autotune.png`

This is not a full autonomous runner/full-suite PASS.

Negative autonomous fixture coverage rejects:

- Agent self-verdict.
- direct API operation.
- CLI nonzero exit.
- unresolved P1.
- failed score item.
- missing artifact.

## Repaired Build Failure

The original `cd frontend && pnpm build` failed with:

- `src/components/MemoryPanel.test.ts(31,54)`: `exists` missing on
  `Omit<DOMWrapper<Element>, "exists">`.
- `src/components/MemoryPanel.test.ts(47,54)`: same.
- `src/components/TimelinePanel.test.ts(35,54)`: same.
- `src/components/TimelinePanel.vue(30,12)`: `rowProps` function type not
  assignable to `GetComponentProps<any>`.
- `src/components/WorldPanel.test.ts(141,64)`: `exists` missing on
  `Omit<DOMWrapper<Element>, "exists">`.

Repair package:
`docs/iterations/v0.4-post-closeout/03-frontend-build-type-repair/`.

Repair summary:

- `get(...).exists()` presence assertions were changed to
  `find(...).exists()` in the reported Vue Test Utils test sites.
- `TimelinePanel.vue` keeps `data-test="timeline-row"` and adds a
  type-compatible no-op row prop for Ant Design Vue `customRow`.
- Read-only frontend type/build reviewer reported no P0/P1/P2/P3 findings for
  the scoped repair.
- Read-only scope/evidence evaluator reported no P0/P1/P2 findings and
  confirmed final closeout may mark clean pass.

## Findings

- P1: none after the repair rerun.
- P2: none blocking this clean-pass validation. Full autonomous
  runner/full-suite coverage is still absent and remains an out-of-scope
  follow-up; current autonomous evidence is a minimal saved-result checker
  validation only.
- P3: stale `dashboard-invalid-param.png` remains in latest smoke screenshots,
  but current `result.json` references `dashboard-agent-autotune.png` and the
  checker passed.
- P3: E2E still uses serial shared local world state; current tests reset or
  cross-check relevant state, but isolated per-test world state remains future
  hardening.

## Verdict

Clean pass after repair rerun.

The frontend build P1 was repaired and the required validation matrix reran
successfully in the current session. The scope/evidence evaluator reported no
blocking findings.
