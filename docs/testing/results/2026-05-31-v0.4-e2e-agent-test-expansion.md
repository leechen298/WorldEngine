# v0.4 E2E And Agent Test Expansion Result

Date: 2026-05-31
Scope: `docs/iterations/v0.4-post-closeout/01-e2e-agent-test-expansion`
Status: passed

## Worktree State

This validation ran against the current local `v0.4` worktree, which already
contained uncommitted v0.4 implementation and documentation changes. This
result is evidence for the current candidate worktree, not for an immutable
committed revision.

## Commands Run

```bash
cd frontend && pnpm exec playwright test e2e/agent-loop.spec.ts
```

Result:

```text
5 passed (1.1s)
```

```bash
cd frontend && pnpm exec playwright test e2e/dashboard.spec.ts -g "dashboard-agent-autotune"
```

Result:

```text
1 passed (2.0s)
```

```bash
cd backend && .venv/bin/python -m pytest ../tools/testing/test_validate_agent_smoke_result.py -q
```

Result:

```text
25 passed in 0.07s
```

```bash
make validate-agent-smoke-fixtures
```

Result:

```text
PASS: validated agent smoke result at tools/testing/fixtures/agent-smoke/valid-basic-runtime
PASS: validated agent smoke result at tools/testing/fixtures/agent-smoke/valid-params-flow
PASS: validated agent smoke result at tools/testing/fixtures/agent-smoke/valid-invalid-param
PASS: validated agent smoke result at tools/testing/fixtures/agent-smoke/valid-agent-autotune
FAIL: verdict_source must be deterministic_checker, not agent
invalid-agent-verdict fixture failed as expected.
25 passed in 0.06s
```

```bash
make test-e2e
```

Result:

```text
11 passed (6.4s)
```

```bash
cd backend && .venv/bin/python -m pytest app/tests/test_agent_loop_service.py app/tests/test_agent_loop_api.py app/tests/test_params_agent.py app/tests/test_event_api_compat.py app/tests/test_runtime_step.py -q
```

Result:

```text
35 passed in 0.45s
```

```bash
make validate-agent-smoke-result RESULT_DIR=test-results/agent-smoke/latest
```

Result:

```text
PASS: validated agent smoke result at test-results/agent-smoke/latest
```

## Coverage Summary

- Browser E2E now covers the v0.4 Agent Loop API through
  `frontend/e2e/agent-loop.spec.ts`.
- Agent Loop API E2E includes noop, valid params patch, reserved-path rejected
  params patch, unsupported action, and schema-error paths.
- Dashboard E2E now verifies that Auto-Tune remains on the existing
  params-agent compatibility path and emits `source="agent.params"`.
- Agent smoke checker/fixtures now support `dashboard-agent-autotune`.
- A live Agent UI/CLI smoke run for `dashboard-agent-autotune` passed
  deterministic validation.

## Artifacts

- E2E artifacts: `test-results/e2e/`
- Agent smoke latest result: `test-results/agent-smoke/latest/`
- Agent smoke screenshot:
  `test-results/agent-smoke/latest/screenshots/dashboard-agent-autotune.png`
- Agent smoke operation log:
  `test-results/agent-smoke/latest/operation-log.jsonl`
- Agent smoke deterministic summary:
  `test-results/agent-smoke/latest/api-summary.json`

## Skipped Checks

- Full scorecard-based Codex/test-runner autonomous validation was not run.
  The repository still treats `docs/testing/agent-autonomous/**` as
  `contract-only-do-not-execute` until a broader scorecard checker exists.

## Findings

- P1: none.
- P2: none.
- P3: stale screenshot file may remain in `test-results/agent-smoke/latest/`
  history/worktree from the previous invalid-param latest record, but the
  current `result.json` references the new Auto-Tune screenshot and the
  deterministic checker passed.

## Final Assessment

passed
