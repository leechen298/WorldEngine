# v0.1 Test Map

Status: current test coverage map

This document maps current tests to implemented v0.1 behavior. It is not a
coverage percentage report.

Latest closeout run:

- backend: `63 passed in 2.93s`.
- frontend: `24 passed`.
- frontend build: passed with a Vite chunk-size warning.
- E2E hardening package: `make test-e2e` passed with `3 passed`.
- Agent smoke checker fixtures: `make validate-agent-smoke-fixtures` passed.
- Current-code test case expansion: scenario contracts only; no new E2E,
  live Agent smoke, or Codex autonomous test was run or reported as passed.
- Remaining current-code coverage: `make test-e2e` passed with `6 passed`;
  `dashboard-invalid-param` live Agent smoke passed deterministic validation.
  Later v0.4 post-closeout evidence added Agent Loop E2E and
  `dashboard-agent-autotune` live Agent smoke.

See `docs/testing/results/2026-05-23-v0.1-closeout.md`.
See also `docs/testing/results/2026-05-23-v0.1-e2e-agent-acceptance.md`.
See also `docs/testing/results/2026-05-24-v0.1.8-params-flow-live-smoke.md`.

## Backend Tests

### `backend/app/tests/test_runtime_step.py`

Covers:

- initial runtime state.
- runtime step increments tick and world time.
- `tick.advanced` event append.
- `WORLD_STEP_SECONDS` env handling.
- `/runtime/state`.
- `/runtime/step`.
- `/health`.
- `/world/events`.
- event filters.
- event cursor pagination.
- `/world/event-steps`.
- grouped event-step cursor pagination.
- unknown cursor errors.
- validation error envelope.
- not-found error envelope.

### `backend/app/tests/test_world_modules.py`

Covers:

- module events in world events.
- counter module increments across ticks.
- module path in event payload.
- stable default module paths.
- composite module child ordering and aggregate event.

### `backend/app/tests/test_world_params.py`

Covers:

- structured params affecting counter increment.
- disabling heartbeat through `heartbeat.enabled`.
- runtime step includes params snapshot.
- `params.applied` event append.
- plain-value params still affecting counter increment.

### `backend/app/tests/test_param_validator.py`

Covers:

- unknown path rejection.
- reserved prefix rejection.
- type mismatch rejection.
- out-of-range rejection.
- missing value rejection.
- invalid op rejection.
- valid patch application.
- invalid patch API error response shape.

### `backend/app/tests/test_dry_run_validation.py`

Covers:

- dry-run rejection of high counter increments.
- dry-run acceptance of reasonable increments.
- static and dry-run error-shape consistency.
- dry-run isolation from active module state.
- dry-run metrics in error responses.

### `backend/app/tests/test_dry_run_policy.py`

Covers:

- env-based dry-run policy.
- world override precedence.
- validation override metrics.
- env max-final-counter metrics.

### `backend/app/tests/test_archive_snapshot_summary.py`

Covers:

- snapshot interval behavior.
- snapshot contents.
- summary interval behavior.
- summary type counts.
- summary text.
- counter details in summaries.
- snapshot list/detail APIs.
- summary list/detail APIs.
- not-found behavior.
- list response shape.

### `backend/app/tests/test_params_agent.py`

Covers:

- agent applies valid patch.
- agent retries after validation failure.
- agent rejects after max attempts.
- invalid LLM JSON handling.

## Frontend Tests

### `frontend/src/api/client.test.ts`

Covers:

- API envelope parsing.
- runtime/client calls.
- error handling through `ApiClientError`.
- query construction for event APIs.

### `frontend/src/components/RuntimeControls.test.ts`

Covers:

- step button behavior.
- stepped event emission and error handling.

### `frontend/src/components/TimelinePanel.test.ts`

Covers:

- timeline rendering.
- step summaries.
- detail formatting.
- pagination controls.
- page-size changes.

### `frontend/src/components/WorldPanel.test.ts`

Covers:

- param rendering.
- manual patch construction.
- validation error display.
- agent auto-tune success and error display.

### `frontend/src/pages/DashboardPage.test.ts`

Covers:

- dashboard initial data loading.
- refresh behavior after runtime step.

## Known Test Gaps

- Browser E2E covers dashboard runtime, params, invalid params, archive
  summary, agent auto-tune, and timeline navigation flows.
- `dashboard-params-flow` has 0.1.8 live Agent smoke evidence, preserved by a
  durable summary and commit reference; `dashboard-invalid-param` has 0.1.9
  live Agent smoke evidence. The current `test-results/agent-smoke/latest/`
  evidence points to the later `dashboard-agent-autotune` run.
- API curl smoke is intentionally not part of the current Agent/Codex testing
  plan; deterministic API evidence may appear only inside Playwright assertions
  or checker artifacts.
- No full Agent autonomous scenario suite or autonomous runner verdict source
  exists. A minimal saved-result scorecard checker can validate recorded
  autonomous evidence.
- No persistence/restart behavior test.
- No recursive world, WorldSpec, or WorldCell tests.
- No agent memory or pseudo-self tests.

## Browser E2E

### `frontend/e2e/dashboard.spec.ts`

Covers:

- dashboard health render.
- runtime step through UI with API tick comparison.
- timeline evidence after stepping.
- valid `counter.increment` params flow through UI, API, and event evidence.
- invalid params flow with UI error and unchanged API params.
- params-agent Auto-Tune through UI with actual patch-value assertions.
- timeline page-size, next/previous pagination, and expanded event detail
  evidence after generating events inside the test.
- archive summary creation and MemoryPanel rendering with before/after latest
  summary comparison.

## Current-Code Scenario Contracts

Scenario contracts live under `docs/testing/e2e-scenarios/`,
`docs/testing/agent-smoke/scenarios/`, and
`docs/testing/agent-autonomous/`.

| Current Capability | E2E Scenario | Agent Smoke Scenario | Codex/Test-Runner Autonomous Scenario | Current Status |
|---|---|---|---|---|
| backend health | `dashboard-basic-runtime` | `dashboard-basic-runtime` | `autonomous-dashboard-basic-runtime` | E2E implemented; Agent smoke executable by validator; minimal autonomous saved-result checker can validate recorded evidence. |
| runtime step | `dashboard-basic-runtime` | `dashboard-basic-runtime` | `autonomous-dashboard-basic-runtime` | E2E implemented; Agent smoke executable by validator; minimal autonomous saved-result checker can validate recorded evidence. |
| timeline event evidence | `dashboard-basic-runtime`, `dashboard-timeline-navigation` | `dashboard-basic-runtime` | `autonomous-dashboard-timeline-investigation` | E2E implemented, including timeline navigation pagination and expanded-detail coverage; minimal autonomous saved-result checker can validate recorded evidence. |
| valid params | `dashboard-params-flow` | `dashboard-params-flow` | `autonomous-dashboard-params-flow` | E2E implemented; Agent smoke 0.1.8 live status remains durable through `docs/testing/results/2026-05-24-v0.1.8-params-flow-live-smoke.md` and commit `c6da552`; minimal autonomous saved-result checker can validate recorded evidence. |
| invalid params | `dashboard-invalid-param` | `dashboard-invalid-param` | `autonomous-dashboard-invalid-param` | E2E implemented; Agent smoke live-smoke-recorded status remains durable; minimal autonomous saved-result checker can validate recorded evidence. |
| params-agent auto-tune | `dashboard-agent-autotune` | `dashboard-agent-autotune` | `autonomous-dashboard-agent-autotune` | E2E implemented with actual patch-value assertions; current latest Agent smoke covers this scenario; minimal autonomous saved-result checker can validate recorded evidence. |
| v0.4 Agent Loop API | `agent-loop-step` | not applicable | future | E2E implemented with API-level Playwright assertions for noop, params patch, rejected actions, schema errors, and boundary paths. |
| archive summary | `dashboard-archive-summary` | not first-batch smoke | future | E2E implemented with before/after latest-summary assertions and Playwright-scoped low archive intervals. |
| full Agent autonomous | not applicable | not applicable | protocol + scorecard | Minimal saved-result checker exists; broad autonomous runner/full suite still not implemented. |

## Agent Smoke Checker

### `tools/testing/test_validate_agent_smoke_result.py`

Covers:

- valid Agent smoke fixture acceptance.
- `dashboard-params-flow` fixture acceptance.
- `dashboard-invalid-param` fixture acceptance.
- `verdict_source = agent` rejection.
- missing required artifact rejection.
- missing `operation-log.jsonl` rejection.
- empty `operation-log.jsonl` rejection.
- direct API operation rejection.
- missing scenario-required UI target rejection.
- `result.json.scenario` / `api-summary.json.scenario` mismatch rejection.
- scenario-specific checker evidence rejection.
- empty command rejection.
- empty assertion rejection.
- assertion evidence requirement.

Current Agent smoke validator-supported scenarios:

- `dashboard-basic-runtime`
- `dashboard-params-flow`
- `dashboard-invalid-param`
- `dashboard-agent-autotune`

`dashboard-basic-runtime` has prior live smoke evidence.
`dashboard-params-flow` has 0.1.8 live smoke evidence preserved through
`docs/testing/results/2026-05-24-v0.1.8-params-flow-live-smoke.md` and commit
`c6da552`. `dashboard-invalid-param` has 0.1.9 live smoke evidence. Current
`test-results/agent-smoke/latest/` points to the later
`dashboard-agent-autotune` live smoke run.
