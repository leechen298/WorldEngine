# v0.3 Plan

状态：`planned / in progress`

## Version Name

WorldSpec Loader and Runtime Bridge

## Objective

把 WorldEngine 从 v0.2 recursive schema foundation 推进到 validated generic
WorldSpec loading 和 runtime context bridging，同时保持 v0.1 runtime
compatibility。

## Version Boundaries

v0.3 可以定义或实现：

- WorldSpec loader contract。
- minimal generic WorldSpec loader。
- runtime context bridge contract。
- minimal optional runtime context bridge。
- runtime/API/event/archive/params/frontend-facing/legacy-path compatibility
  evidence。
- external fixture runner contract readiness。
- evidence and compatibility audit。
- release-candidate 和 final closeout docs。

v0.3 不可以实现：

- Agent-in-World loop。
- memory 或 self-continuity substrate。
- world generation。
- 面向 external product surface 的 projection API。
- product UI 或 game UI。
- concrete demo world fixture。
- concrete external validation world。
- external fixture repository。
- external validation repository。
- story generation。
- NPC chat system。
- self-awareness claims。

## Required Compatibility Baseline

任何后续 v0.3 package 在修改 runtime、API、event、archive、params、
frontend-facing 或 legacy-path behavior 前，必须提供 current-session
compatibility evidence，覆盖：

- `RuntimeEngine` tick 和 `world_time_seconds` behavior。
- API envelope 和 error shape。
- `/runtime/step`。
- `/world/events`。
- `/world/event-steps`。
- world params 和 params apply behavior。
- archive snapshot 和 summary behavior。
- optional `Event.refs` response compatibility。
- frontend-facing response shapes。
- legacy `backend/worldengine/` boundary。

## Planned Packages

### 0.3.0 v0.3 Planning And Compatibility Baseline

Package name: `0.3.0-v0.3-planning-and-compatibility-baseline`

Status: ready for review

Type: documentation-only

Goal: 建立 v0.3 iteration docs 和 compatibility baseline，不实现 loader 或 bridge behavior。

Why this exists: v0.3 是第一次把 WorldSpec 推近 runtime 的版本，因此需要先建立边界、roadmap 状态同步、package sequence 和 compatibility evidence requirements。

Inputs / required reading:

- `AGENTS.md`
- `AGENTS.zh.md`
- `README.md`
- `README.zh.md`
- `docs/project-north-star.md`
- `docs/project-north-star.zh.md`
- `docs/product-model.md`
- `docs/product-model.zh.md`
- `docs/scope-boundaries.md`
- `docs/scope-boundaries.zh.md`
- `docs/roadmap.md`
- `docs/roadmap.zh.md`
- `docs/current-implementation.md`
- `docs/current-implementation.zh.md`
- `docs/backend-implementation.md`
- `docs/backend-implementation.zh.md`
- `docs/architecture.md`
- `docs/architecture.zh.md`
- `docs/external-fixture-boundary.md`
- `docs/validation-report-template.md`
- `docs/releases/v0.2.md`
- `docs/releases/v0.2.zh.md`
- `docs/iterations/README.md`
- `docs/iterations/v0.2/README.md`
- `docs/iterations/v0.2/README.zh.md`
- `docs/iterations/v0.2/v0.2-plan.md`
- `docs/iterations/v0.2/v0.2-plan.zh.md`
- `docs/iterations/v0.2/0.2.12-v0.2-final-closeout/review.md`
- `backend/app/core/runtime_engine.py`
- `backend/app/schemas/world_cell.py`
- `backend/app/schemas/event.py`
- `backend/app/tests/test_worldspec_schema_smoke.py`
- `backend/app/tests/test_event_api_compat.py`
- `backend/app/tests/test_event_schema_compat.py`

Allowed changes:

- 创建 v0.3 iteration docs。
- 创建 0.3.0 package docs。
- 如果 roadmap 的 v0.2 status 仍与 final closeout 不一致，则更新 wording。
- 新增 v0.3 release placeholder docs。
- 新增 compatibility baseline requirements。

Forbidden changes:

- runtime changes。
- schema changes。
- API changes。
- frontend changes。
- backend test changes。
- fixture changes。
- loader implementation。
- bridge implementation。
- agent、memory、generation 或 projection implementation。
- external repository creation。
- concrete demo world details。

Expected deliverables:

- `docs/iterations/v0.3/README.md`
- `docs/iterations/v0.3/README.zh.md`
- `docs/iterations/v0.3/v0.3-plan.md`
- `docs/iterations/v0.3/v0.3-plan.zh.md`
- `docs/iterations/v0.3/00-chatgpt-plan.md`
- `docs/iterations/v0.3/development-workflow.md`
- `docs/iterations/v0.3/final-review-bundle-template.md`
- `docs/iterations/v0.3/0.3.0-v0.3-planning-and-compatibility-baseline/**`
- `docs/releases/v0.3.md`
- `docs/releases/v0.3.zh.md`
- 如需要，同步 roadmap status。

Expected tests / verification:

- `git status --short --branch`
- `git diff --check`
- required v0.3 docs file existence checks。
- targeted status and package-sequence grep checks。
- forbidden completion / release wording grep，期望无结果。
- roadmap status acceptance gate。

Compatibility constraints:

- 本包是 documentation-only。
- runtime、schema、API、frontend、tests、fixtures 和 legacy behavior 必须保持不变。
- v0.2 保持 final / closeout complete。
- v0.3 保持 planned / in progress。

Scope guardrails:

- 不写 loader 或 bridge code。
- 不扩大到 root README status cleanup。
- 不引入 external fixture internals。
- future packages 只作为 planned specs，不作为 delivered behavior claims。

Exit criteria:

- v0.3 docs 存在。
- 0.3.0 package docs 存在。
- v0.2 roadmap status drift 被移除。
- v0.3 release placeholders 存在且不是 release announcement。
- 0.3.0 `review.md` 记录 commands、results、compatibility review、scope review、unresolved findings 和 final assessment。

Handoff to next package:

0.3.0 通过 review 后，0.3.1 可以定义 WorldSpec loader contract。

### 0.3.1 WorldSpec Loader Contract

Package name: `0.3.1-worldspec-loader-contract`

Status: planned

Type: documentation-only

Goal: 在实现 loader code 前定义 WorldSpec loader contract。

Why this exists: Loader implementation 需要先有 review 过的输入、输出、error model、validation behavior 和 domain-neutral fixture policy。

Inputs / required reading:

- 0.3.0 package review。
- `docs/contracts/worldspec-contract.md`
- `backend/app/schemas/world_cell.py`
- `backend/app/tests/test_worldspec_schema_smoke.py`
- `docs/scope-boundaries.md`
- `docs/external-fixture-boundary.md`
- `docs/iterations/v0.2/compatibility-review.md`

Allowed changes:

- 新增 `docs/contracts/worldspec-loader-contract.md`。
- 创建 0.3.1 package docs。
- 定义 loader input、output、error 和 validation semantics。
- 定义 domain-neutral example policy，不添加 concrete fixture data。
- 定义 loader 与 runtime 分离的 compatibility expectations。

Forbidden changes:

- 不实现 loader。
- 不连接 runtime。
- 不创建 concrete WorldSpec fixture data。
- 不创建 external fixture repository。
- 不改变 API routes 或 response shapes。
- 不修改 RuntimeEngine behavior。

Expected deliverables:

- `docs/contracts/worldspec-loader-contract.md`
- `docs/iterations/v0.3/0.3.1-worldspec-loader-contract/**`
- review evidence。

Expected tests / verification:

- `git diff --check`
- contract field / heading checks。
- concrete demo anchor sweep over touched docs。
- 不运行 backend/frontend tests，除非误触 implementation files；如果误触则停止并记录 scope violation。

Compatibility constraints:

- Loader contract 只接受 generic WorldSpec data。
- Loader contract 不暗示 runtime behavior changes。
- Schema compatibility 保持 additive，除非后续 reviewed package 另有规定。

Scope guardrails:

- 只做 contract 和 planning work。
- 不把 runtime bridge design 偷渡成 loader implementation。
- examples 保持 domain-neutral。

Exit criteria:

- Loader contract 足够具体，implementation 不需要发明 policy。
- Error 和 validation expectations 明确。
- Runtime 保持 untouched。

Handoff to next package:

0.3.2 可以基于 review 过的 contract 实现 minimal loader。

### 0.3.2 WorldSpec Loader Implementation

Package name: `0.3.2-worldspec-loader-implementation`

Status: planned

Type: mixed or code

Goal: 实现 minimal generic WorldSpec loader。

Why this exists: Contract review 后，WorldEngine 需要一个只负责加载和验证 generic WorldSpec data 的小 loader，不接 RuntimeEngine。

Inputs / required reading:

- 0.3.1 loader contract and review。
- `docs/contracts/worldspec-loader-contract.md`
- `backend/app/schemas/world_cell.py`
- `backend/app/tests/test_worldspec_schema_smoke.py`
- `docs/scope-boundaries.md`
- `docs/external-fixture-boundary.md`

Allowed changes:

- 新增 `backend/app/core/worldspec_loader.py`。
- 新增 domain-neutral loader tests。
- 仅在 loader contract 允许时新增 generic error types 或 return structures。
- 更新 package review evidence。

Forbidden changes:

- 不改变 `RuntimeEngine.step` behavior。
- 不接 API routes，除非 reviewed package contract 明确允许。
- 不加载 concrete demo world fixtures。
- 不创建 generated world lifecycle behavior。
- 不添加 persistence model。
- 不创建 external fixture repository。

Expected deliverables:

- `backend/app/core/worldspec_loader.py`
- `backend/app/tests/test_worldspec_loader.py`
- `docs/iterations/v0.3/0.3.2-worldspec-loader-implementation/**`
- review evidence。

Expected tests / verification:

- `git diff --check`
- focused loader pytest。
- 如使用 schema validation，运行 focused schema smoke pytest。
- 根据 blast radius 运行 backend app tests。
- concrete demo anchor sweep over touched docs and tests。

Compatibility constraints:

- Existing schema tests 必须继续通过。
- Runtime、API、frontend、params、archive、event 和 legacy behavior 必须不变。
- Loader errors 必须遵循 reviewed contract semantics。

Scope guardrails:

- Loader 只做 data loading 和 validation。
- 不实现 bridge、generation、agent loop、memory、projection 或 external runner behavior。
- Loader inputs 保持 domain-neutral。

Exit criteria:

- Loader 能 validate generic WorldSpec data。
- Invalid input behavior 有测试。
- Runtime 和 API behavior 不变。

Handoff to next package:

0.3.3 可以定义 runtime context bridge contract。

### 0.3.3 Runtime Context Bridge Contract

Package name: `0.3.3-runtime-context-bridge-contract`

Status: planned

Type: documentation-only

Goal: 定义 RuntimeEngine 如何接收 validated WorldSpec-derived runtime context，但不实现 bridge。

Why this exists: Runtime bridge 会触碰 v0.1 compatibility surfaces，因此必须先定义 context shape、compatibility rules 和 runtime boundaries。

Inputs / required reading:

- 0.3.2 loader review，如已存在。
- `backend/app/core/runtime_engine.py`
- `backend/app/core/event_bus.py`
- `backend/app/world/modules/*`
- `docs/current-implementation.md`
- `docs/backend-implementation.md`
- `docs/iterations/v0.2/compatibility-review.md`

Allowed changes:

- 新增 `docs/contracts/runtime-context-bridge-contract.md`。
- 创建 0.3.3 package docs。
- 定义 runtime context input shape。
- 定义 bridge behavior changes 前需要的 compatibility evidence。
- 定义其与 tick、world time、event log 和 default module tree 的关系。

Forbidden changes:

- 不实现 bridge。
- 不改变 RuntimeEngine behavior。
- 不修改 API response shape。
- 不把 raw WorldSpec 放入 event payloads。
- 不引入 concrete world logic。
- 不把 WorldCell 直接等同于 runtime module。

Expected deliverables:

- `docs/contracts/runtime-context-bridge-contract.md`
- `docs/iterations/v0.3/0.3.3-runtime-context-bridge-contract/**`
- review evidence。

Expected tests / verification:

- `git diff --check`
- contract heading / term checks。
- compatibility-baseline wording checks。
- concrete demo anchor sweep over touched docs。

Compatibility constraints:

- 必须明确保持 v0.1 runtime tick、world time、event log、params、archive、API、frontend 和 legacy boundaries。
- Runtime context 是 bridge contract，不是 runtime migration。

Scope guardrails:

- 本包不实现 bridge。
- 不设计 agent loop、memory、generation、projection 或 external validation internals。

Exit criteria:

- Bridge contract 足以实现，context semantics 不需要猜。
- Required compatibility evidence 明确。
- 不发生 runtime behavior changes。

Handoff to next package:

0.3.4 可以实现 minimal runtime context bridge。

### 0.3.4 Runtime Context Bridge Implementation

Package name: `0.3.4-runtime-context-bridge-implementation`

Status: planned

Type: mixed or code

Goal: 从 validated generic WorldSpec-derived data 实现 minimal optional runtime context bridge。

Why this exists: Loader 和 bridge contracts review 后，runtime 需要一个小的 optional context path，同时不破坏现有 tick、world time、event log、params、archive、API 或 frontend behavior。

Inputs / required reading:

- 0.3.3 bridge contract and review。
- 0.3.2 loader implementation and review。
- `backend/app/core/runtime_engine.py`
- `backend/app/core/event_bus.py`
- `backend/app/world/modules/*`
- runtime、events、params、archive compatibility tests。

Allowed changes:

- 新增 `backend/app/core/runtime_context.py`。
- 新增 bridge module 或 helper。
- 在保持 existing behavior 的前提下，让 RuntimeEngine 持有 optional runtime context。
- 新增 compatibility tests。
- 更新 package review evidence。

Forbidden changes:

- 不破坏 `/world/step`、`/world/events` 或 `/world/event-steps` legacy response shapes。
- 不把整个 RuntimeEngine 迁移到 WorldCell。
- 不实现 world generation。
- 不实现 Agent action loop。
- 不改变 default runtime scaffold behavior。
- 不引入 concrete demo world details。

Expected deliverables:

- `backend/app/core/runtime_context.py`
- reviewed contract 允许的 focused bridge code changes。
- `backend/app/tests/test_runtime_context_bridge.py`
- `docs/iterations/v0.3/0.3.4-runtime-context-bridge-implementation/**`
- compatibility evidence。

Expected tests / verification:

- `git diff --check`
- focused runtime context bridge pytest。
- event API compatibility pytest。
- contract 要求的 runtime/API/params/archive compatibility checks。
- 根据 blast radius 运行 backend app tests。
- concrete demo anchor sweep。

Compatibility constraints:

- Existing runtime tick 和 `world_time_seconds` behavior 必须保持 compatible。
- API envelopes 和 existing response shapes 必须保持 compatible。
- Optional `Event.refs` behavior 必须保持 additive。
- Frontend-facing behavior 不得 regress。

Scope guardrails:

- Bridge 保持 minimal 和 optional。
- 不把 WorldCell 转成 runtime module semantics。
- 不添加 generation、persistence、agent loop、memory、projection 或 concrete world behavior。

Exit criteria:

- Runtime 可以通过 approved bridge shape 持有或接收 validated context。
- Existing compatibility tests 通过。
- Review evidence 映射所有 compatibility surfaces。

Handoff to next package:

0.3.5 可以准备 external fixture runner contract readiness。

### 0.3.5 External Fixture Contract Readiness

Package name: `0.3.5-external-fixture-contract-readiness`

Status: planned

Type: documentation-only or mixed

Goal: 定义 external fixture runners 如何通过 public contracts 消费 WorldEngine，不在 core 内创建 external repositories。

Why this exists: External validation 是 consumer boundary。v0.3 需要 public runner contracts 和 redacted evidence expectations，但不能导入 concrete external world internals。

Inputs / required reading:

- `docs/external-fixture-boundary.md`
- `docs/validation-report-template.md`
- 已完成的 0.3 loader 和 bridge package reviews。
- public API / CLI / schema / contract docs。

Allowed changes:

- 新增 `docs/contracts/external-fixture-runner-contract.md`。
- 创建 0.3.5 package docs。
- 定义 public API / CLI / schema / exported-contract consumption paths。
- 定义 redacted validation report expectations。
- 添加只使用 abstract identifiers 的 documentation-only examples。

Forbidden changes:

- 不创建 external repositories。
- 不添加 concrete fixture data。
- 不添加 concrete external world names。
- 不添加 UI selectors、reset API internals 或 private oracle behavior。
- 不实现 product validation app。

Expected deliverables:

- `docs/contracts/external-fixture-runner-contract.md`
- `docs/iterations/v0.3/0.3.5-external-fixture-contract-readiness/**`
- review evidence。

Expected tests / verification:

- `git diff --check`
- contract heading checks。
- redaction rule checks。
- concrete demo anchor sweep。
- 不运行 backend/frontend tests，除非 implementation scope 被明确加入。

Compatibility constraints:

- External fixtures 保持 consumers。
- WorldEngine core 只存 public contracts 和 redacted reports。
- Loader 和 bridge APIs 不得被单个 consumer 收窄。

Scope guardrails:

- 所有 examples 保持 domain-neutral。
- 不用 external validation worlds 反向驱动 internal abstractions。
- 不添加 product UI 或 validation app logic。

Exit criteria:

- External fixture runner contract 足够清楚，可由单独 repository 消费 public WorldEngine behavior。
- Redacted report boundary 明确。
- 未添加 external internals。

Handoff to next package:

0.3.6 可以审计 loader、bridge、external fixture readiness 和 compatibility evidence。

### 0.3.6 Runtime Bridge Evidence And Compatibility Audit

Package name: `0.3.6-runtime-bridge-evidence-and-compatibility-audit`

Status: planned

Type: documentation-only or mixed

Goal: 汇总 loader 和 bridge evidence，审计 compatibility，并判断是否可 handoff 到 v0.4 Agent-in-World minimal loop。

Why this exists: Release-candidate 前，v0.3 需要 evidence index 区分 implemented behavior、documented contracts、tested compatibility、missing evidence 和 blockers。

Inputs / required reading:

- all completed v0.3 package reviews。
- loader contract and implementation evidence。
- bridge contract and implementation evidence。
- external fixture runner contract readiness docs。
- v0.2 compatibility review。

Allowed changes:

- 新增 `docs/iterations/v0.3/evidence-index.md`。
- 新增 `docs/iterations/v0.3/compatibility-audit.md`。
- 创建 0.3.6 package docs。
- 记录 findings 和 handoff readiness。
- 添加 documentation-only status links。

Forbidden changes:

- 不添加新功能。
- 不隐藏 P1 或 P2 findings。
- blocker 仍存在时不声明 final release status。
- 不修补 loader 或 bridge behavior，除非 package 明确升级为 reviewed mixed scope。

Expected deliverables:

- `docs/iterations/v0.3/evidence-index.md`
- `docs/iterations/v0.3/compatibility-audit.md`
- `docs/iterations/v0.3/0.3.6-runtime-bridge-evidence-and-compatibility-audit/**`
- review evidence。

Expected tests / verification:

- `git diff --check`
- evidence traceability checks。
- compatibility surface grep checks。
- concrete demo anchor sweep。
- 不运行 backend/frontend tests，除非 implementation files 被 reviewed mixed scope 修改。

Compatibility constraints:

- Audit 必须记录实际 current-session evidence，而不是 assumed behavior。
- API、runtime、event、archive、params、frontend-facing、schema、fixture 和 legacy impacts 必须分类。

Scope guardrails:

- Gaps 作为 findings 或 next-package inputs。
- 不用 unreviewed feature work 填补 evidence gaps。
- 不启动 v0.4 implementation。

Exit criteria:

- Evidence index 和 compatibility audit 存在。
- P1/P2/P3 findings 明确。
- v0.4 handoff readiness 被诚实分类。

Handoff to next package:

如果没有 blocking findings，0.3.7 可以准备 release-candidate bundle。

### 0.3.7 v0.3 Release Candidate Bundle

Package name: `0.3.7-v0.3-release-candidate-bundle`

Status: planned

Type: documentation-only

Goal: 汇总 v0.3 planned、implemented、tested 和 not implemented 状态，供 human / ChatGPT final review。

Why this exists: Release candidate 是 evidence bundle，不声明 release status，也不补 missing functionality。

Inputs / required reading:

- all v0.3 package reviews through 0.3.6。
- `docs/iterations/v0.3/evidence-index.md`。
- `docs/iterations/v0.3/compatibility-audit.md`。
- v0.3 release placeholder docs。
- roadmap and scope boundaries。

Allowed changes:

- 创建 0.3.7 package docs。
- 新增 `docs/iterations/v0.3/v0.3-release-candidate-bundle.md`。
- 汇总 evidence 和 limitations。
- 准备 final review bundle。
- 记录 unresolved findings。

Forbidden changes:

- 不声明 release status。
- 不添加 features。
- 不隐藏 P1/P2 findings。
- 不修改 runtime、schema、API、frontend、tests、fixtures 或 legacy behavior。
- 不添加 concrete external-world details。

Expected deliverables:

- `docs/iterations/v0.3/0.3.7-v0.3-release-candidate-bundle/**`
- `docs/iterations/v0.3/v0.3-release-candidate-bundle.md`
- final review bundle。
- review evidence。

Expected tests / verification:

- `git diff --check`
- release-candidate wording checks。
- evidence traceability checks。
- unresolved finding checks。
- concrete demo anchor sweep。

Compatibility constraints:

- Candidate docs 只能反映 actual evidence。
- P1/P2 findings 必须保持可见。
- Release placeholder status 必须保持 planned / not released。

Scope guardrails:

- 只 bundle evidence。
- 不在 release-candidate package 中修 missing functionality。
- v0.4 只作为 handoff，不实现。

Exit criteria:

- Release-candidate bundle ready for human / ChatGPT review。
- 不声明 final release status。
- Findings 可见且有 severity 分类。

Handoff to next package:

0.3.8 只能在 release-candidate review approval 后执行 final closeout。

### 0.3.8 v0.3 Final Closeout

Package name: `0.3.8-v0.3-final-closeout`

Status: planned / gated

Type: documentation-only

Goal: 在 release-candidate review approval 后执行 final closeout。

Why this exists: Final closeout 是 review approval 后的窄 status 和 evidence update，不是 feature 或 cleanup package。

Inputs / required reading:

- 0.3.7 release-candidate bundle。
- human / ChatGPT final review result。
- unresolved findings list。
- roadmap and release docs。
- all v0.3 package reviews。

Allowed changes:

- 创建 0.3.8 package docs。
- 如果 review approval 支持，将 `docs/releases/v0.3.md` 和 `docs/releases/v0.3.zh.md` 更新为 final wording。
- 更新 v0.3 index 和 plan status。
- 记录 final closeout evidence。

Forbidden changes:

- 不添加 features。
- 不绕过 unresolved P1/P2 findings。
- 不声明未运行的 tests。
- 不改变 runtime、schema、API、frontend、tests、fixtures 或 legacy behavior。
- 不添加 concrete external-world details。

Expected deliverables:

- `docs/iterations/v0.3/0.3.8-v0.3-final-closeout/**`
- `docs/releases/v0.3.md`
- `docs/releases/v0.3.zh.md`
- final closeout review evidence。

Expected tests / verification:

- `git diff --check`
- final-status wording checks。
- unresolved P1/P2 blocker checks。
- concrete demo anchor sweep。
- 不运行 backend/frontend tests，除非 implementation files 被 reviewed scope 修改。

Compatibility constraints:

- Final closeout 必须由 release-candidate review evidence 支撑。
- Runtime、schema、API、frontend、tests、fixtures 和 legacy behavior 在 closeout package 中保持不变。

Scope guardrails:

- 不用 closeout patch features。
- 如 P1/P2 findings 仍存在，停止并记录 blockers。
- v0.4 work 只作为 handoff。

Exit criteria:

- 只有 review approval 和 evidence 支持时，才更新 v0.3 closeout status。
- 无 unresolved P1/P2 findings。
- v0.4 handoff 明确。

Handoff to next package:

v0.4 只能通过自己的 reviewed iteration package 启动。
