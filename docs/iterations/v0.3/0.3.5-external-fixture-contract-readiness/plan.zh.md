# Plan

## 步骤

1. 阅读项目治理、范围、路线图、迭代、v0.3 计划、外部样例边界、验证报告模板、
   加载器契约和桥接层契约文档。
2. 创建外部验证样例运行器公开契约。
3. 创建 0.3.5 迭代包文档和中英文镜像。
4. 在包 README、v0.3 里程碑索引和 v0.3 详细计划中将 0.3.5 标为 `待评审`。
5. 运行文档验证检查。
6. 在 `review.md` 记录命令、结果、兼容性评审、范围评审、假设和开放风险。

## 验收标准

- 0.3.5 迭代包文档完整。
- `docs/contracts/external-fixture-runner-contract.md` 存在。
- 公开消费表面限制为 API、CLI 契约、schema、导出契约和脱敏报告。
- 脱敏报告要求可对照 `docs/validation-report-template.md` 测试。
- 具体外部世界细节被禁止。
- 迭代包和 v0.3 索引状态均为 `待评审`。
- 文档验证通过。
- 不宣称实现测试已通过。

## 不计划

- 运行时实现。
- schema 实现。
- API 实现。
- 前端实现。
- 样例实现。
- 迁移实现。
- 测试实现。
- 创建外部仓库。
- 实现产品验证应用。

## 评审门禁

`test-plan.md` 中的检查通过后，本包可进入文档评审。本包不得变成
`ready for implementation`，因为本包没有实现内容。
