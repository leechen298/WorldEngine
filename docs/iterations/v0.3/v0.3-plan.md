# v0.3 Plan

Status: planned / in progress

## Version Name

WorldSpec Loader and Runtime Bridge

## Objective

Move WorldEngine from the v0.2 recursive schema foundation toward validated
generic WorldSpec loading and runtime context bridging while preserving v0.1
runtime compatibility.

## Version Boundaries

v0.3 may define or implement:

- WorldSpec loader contract.
- minimal generic WorldSpec loader.
- runtime context bridge contract.
- minimal optional runtime context bridge.
- runtime/API/event/archive/params/frontend-facing/legacy-path compatibility
  evidence.
- external fixture runner contract readiness.
- evidence and compatibility audit.
- release-candidate and final closeout docs.

v0.3 must not implement:

- Agent-in-World loop.
- memory or self-continuity substrate.
- world generation.
- projection API for external product surfaces.
- product UI or game UI.
- concrete demo world fixture.
- concrete external validation world.
- external fixture repository.
- external validation repository.
- story generation.
- NPC chat system.
- self-awareness claims.

## Required Compatibility Baseline

Before any future v0.3 package changes runtime, API, event, archive, params,
frontend-facing, or legacy-path behavior, that package must provide
current-session compatibility evidence for:

- `RuntimeEngine` tick and `world_time_seconds` behavior.
- API envelope and error shape.
- `/runtime/step`.
- `/world/events`.
- `/world/event-steps`.
- world params and params apply behavior.
- archive snapshot and summary behavior.
- optional `Event.refs` response compatibility.
- frontend-facing response shapes.
- legacy `backend/worldengine/` boundary.

## Planned Packages

### 0.3.0 v0.3 Planning And Compatibility Baseline

Package name: `0.3.0-v0.3-planning-and-compatibility-baseline`

Status: ready for review

Type: documentation-only

Goal: Establish v0.3 iteration docs and compatibility baseline without
implementing loader or bridge behavior.

Why this exists: v0.3 is the first version that moves WorldSpec toward runtime,
so it needs boundaries, roadmap status sync, package sequencing, and
compatibility evidence requirements before implementation work starts.

Inputs / required reading:

- `AGENTS.md`
- `AGENTS.zh.md`
- `README.md`
- `README.zh.md`
- `docs/project-north-star.md`
- `docs/project-north-star.zh.md`
- `docs/product-model.md`
- `docs/product-model.zh.md`
- `docs/scope-boundaries.md`
- `docs/scope-boundaries.zh.md`
- `docs/roadmap.md`
- `docs/roadmap.zh.md`
- `docs/current-implementation.md`
- `docs/current-implementation.zh.md`
- `docs/backend-implementation.md`
- `docs/backend-implementation.zh.md`
- `docs/architecture.md`
- `docs/architecture.zh.md`
- `docs/external-fixture-boundary.md`
- `docs/validation-report-template.md`
- `docs/releases/v0.2.md`
- `docs/releases/v0.2.zh.md`
- `docs/iterations/README.md`
- `docs/iterations/v0.2/README.md`
- `docs/iterations/v0.2/README.zh.md`
- `docs/iterations/v0.2/v0.2-plan.md`
- `docs/iterations/v0.2/v0.2-plan.zh.md`
- `docs/iterations/v0.2/0.2.12-v0.2-final-closeout/review.md`
- `backend/app/core/runtime_engine.py`
- `backend/app/schemas/world_cell.py`
- `backend/app/schemas/event.py`
- `backend/app/tests/test_worldspec_schema_smoke.py`
- `backend/app/tests/test_event_api_compat.py`
- `backend/app/tests/test_event_schema_compat.py`

Allowed changes:

- Create v0.3 iteration docs.
- Create 0.3.0 package docs.
- Update roadmap v0.2 status wording if still inconsistent with final
  closeout.
- Add v0.3 release placeholder docs.
- Add compatibility baseline requirements.

Forbidden changes:

- Runtime changes.
- Schema changes.
- API changes.
- Frontend changes.
- Backend test changes.
- Fixture changes.
- Loader implementation.
- Bridge implementation.
- Agent, memory, generation, or projection implementation.
- External repository creation.
- Concrete demo world details.

Expected deliverables:

- `docs/iterations/v0.3/README.md`
- `docs/iterations/v0.3/README.zh.md`
- `docs/iterations/v0.3/v0.3-plan.md`
- `docs/iterations/v0.3/v0.3-plan.zh.md`
- `docs/iterations/v0.3/00-chatgpt-plan.md`
- `docs/iterations/v0.3/development-workflow.md`
- `docs/iterations/v0.3/final-review-bundle-template.md`
- `docs/iterations/v0.3/0.3.0-v0.3-planning-and-compatibility-baseline/**`
- `docs/releases/v0.3.md`
- `docs/releases/v0.3.zh.md`
- roadmap status sync if needed.

Expected tests / verification:

- `git status --short --branch`
- `git diff --check`
- file existence checks for required v0.3 docs.
- targeted status and package-sequence grep checks.
- forbidden completion / release wording grep with no matches.
- roadmap status acceptance gate.

Compatibility constraints:

- This package is documentation-only.
- Runtime, schema, API, frontend, tests, fixtures, and legacy behavior must
  remain unchanged.
- v0.2 remains final / closeout complete.
- v0.3 remains planned / in progress.

Scope guardrails:

- Do not write loader or bridge code.
- Do not widen into root README status cleanup.
- Do not introduce external fixture internals.
- Keep future packages as planned specs, not claims of delivered behavior.

Exit criteria:

- v0.3 docs exist.
- 0.3.0 package docs exist.
- v0.2 roadmap status drift is removed.
- v0.3 release placeholders exist and are not release announcements.
- 0.3.0 `review.md` records commands, results, compatibility review, scope
  review, unresolved findings, and final assessment.

Handoff to next package:

0.3.1 may define the WorldSpec loader contract after 0.3.0 is reviewed.

### 0.3.1 WorldSpec Loader Contract

Package name: `0.3.1-worldspec-loader-contract`

Status: planned

Type: documentation-only

Goal: Define the WorldSpec loader contract before implementing loader code.

Why this exists: Loader implementation needs a reviewed contract for inputs,
outputs, error model, validation behavior, and domain-neutral fixture policy
before code can safely load WorldSpec data.

Inputs / required reading:

- 0.3.0 package review.
- `docs/contracts/worldspec-contract.md`
- `backend/app/schemas/world_cell.py`
- `backend/app/tests/test_worldspec_schema_smoke.py`
- `docs/scope-boundaries.md`
- `docs/external-fixture-boundary.md`
- `docs/iterations/v0.2/compatibility-review.md`

Allowed changes:

- Add `docs/contracts/worldspec-loader-contract.md`.
- Create 0.3.1 package docs.
- Define loader input, output, error, and validation semantics.
- Define domain-neutral example policy without adding concrete fixture data.
- Define compatibility expectations for keeping loader separate from runtime.

Forbidden changes:

- Do not implement loader.
- Do not connect loader to runtime.
- Do not create concrete WorldSpec fixture data.
- Do not create an external fixture repository.
- Do not change API routes or response shapes.
- Do not modify RuntimeEngine behavior.

Expected deliverables:

- `docs/contracts/worldspec-loader-contract.md`
- `docs/iterations/v0.3/0.3.1-worldspec-loader-contract/**`
- review evidence.

Expected tests / verification:

- `git diff --check`
- contract field / heading checks.
- concrete demo anchor sweep over touched docs.
- no backend/frontend tests unless implementation files are accidentally
  touched, in which case stop and report scope violation.

Compatibility constraints:

- Loader contract must accept only generic WorldSpec data.
- Loader contract must not imply runtime behavior changes.
- Schema compatibility remains additive unless a future reviewed package says
  otherwise.

Scope guardrails:

- Keep this as contract and planning work.
- Do not smuggle runtime bridge design into loader implementation.
- Keep examples domain-neutral.

Exit criteria:

- Loader contract is specific enough for implementation without inventing
  policy.
- Error and validation expectations are explicit.
- Runtime remains untouched.

Handoff to next package:

0.3.2 may implement the minimal loader from the reviewed contract.

### 0.3.2 WorldSpec Loader Implementation

Package name: `0.3.2-worldspec-loader-implementation`

Status: planned

Type: mixed or code

Goal: Implement the minimal generic WorldSpec loader.

Why this exists: After the contract is reviewed, WorldEngine needs a small
loader that can load and validate generic WorldSpec data without connecting it
to RuntimeEngine.

Inputs / required reading:

- 0.3.1 loader contract and review.
- `docs/contracts/worldspec-loader-contract.md`
- `backend/app/schemas/world_cell.py`
- `backend/app/tests/test_worldspec_schema_smoke.py`
- `docs/scope-boundaries.md`
- `docs/external-fixture-boundary.md`

Allowed changes:

- Add `backend/app/core/worldspec_loader.py`.
- Add domain-neutral loader tests.
- Add generic error types or return structures only if the loader contract
  allows them.
- Update package review evidence.

Forbidden changes:

- Do not change `RuntimeEngine.step` behavior.
- Do not connect loader to API routes unless the reviewed package contract
  explicitly allows it.
- Do not load concrete demo world fixtures.
- Do not create generated world lifecycle behavior.
- Do not add persistence model.
- Do not create an external fixture repository.

Expected deliverables:

- `backend/app/core/worldspec_loader.py`
- `backend/app/tests/test_worldspec_loader.py`
- `docs/iterations/v0.3/0.3.2-worldspec-loader-implementation/**`
- review evidence.

Expected tests / verification:

- `git diff --check`
- focused loader pytest.
- focused schema smoke pytest if schema validation is used.
- backend app tests as required by blast radius.
- concrete demo anchor sweep over touched docs and tests.

Compatibility constraints:

- Existing schema tests must keep passing.
- Runtime, API, frontend, params, archive, event, and legacy behavior must be
  unchanged.
- Loader errors must follow reviewed contract semantics.

Scope guardrails:

- Keep loader data-only and validation-only.
- Do not implement bridge, generation, agent loop, memory, projection, or
  external runner behavior.
- Keep loader inputs domain-neutral.

Exit criteria:

- Loader validates generic WorldSpec data.
- Invalid input behavior is tested.
- Runtime and API behavior remain unchanged.

Handoff to next package:

0.3.3 may define the runtime context bridge contract.

### 0.3.3 Runtime Context Bridge Contract

Package name: `0.3.3-runtime-context-bridge-contract`

Status: planned

Type: documentation-only

Goal: Define how RuntimeEngine may receive validated WorldSpec-derived runtime
context without implementing the bridge yet.

Why this exists: Runtime bridge work touches v0.1 compatibility surfaces, so
the contract must define context shape, compatibility rules, and runtime
boundaries before code changes.

Inputs / required reading:

- 0.3.2 loader review, if available.
- `backend/app/core/runtime_engine.py`
- `backend/app/core/event_bus.py`
- `backend/app/world/modules/*`
- `docs/current-implementation.md`
- `docs/backend-implementation.md`
- `docs/iterations/v0.2/compatibility-review.md`

Allowed changes:

- Add `docs/contracts/runtime-context-bridge-contract.md`.
- Create 0.3.3 package docs.
- Define runtime context input shape.
- Define compatibility evidence required before bridge behavior changes.
- Define relationship to tick, world time, event log, and default module tree.

Forbidden changes:

- Do not implement bridge.
- Do not change RuntimeEngine behavior.
- Do not modify API response shape.
- Do not put raw WorldSpec into event payloads.
- Do not introduce concrete world logic.
- Do not equate WorldCell directly with runtime module.

Expected deliverables:

- `docs/contracts/runtime-context-bridge-contract.md`
- `docs/iterations/v0.3/0.3.3-runtime-context-bridge-contract/**`
- review evidence.

Expected tests / verification:

- `git diff --check`
- contract heading / term checks.
- compatibility-baseline wording checks.
- concrete demo anchor sweep over touched docs.

Compatibility constraints:

- v0.1 runtime tick, world time, event log, params, archive, API, frontend, and
  legacy boundaries must be explicitly preserved.
- Runtime context is a bridge contract, not a runtime migration.

Scope guardrails:

- Keep bridge implementation out of this package.
- Do not design agent loop, memory, generation, projection, or external
  validation internals.

Exit criteria:

- Bridge contract is implementable without guessing context semantics.
- Required compatibility evidence is explicit.
- No runtime behavior changes occur.

Handoff to next package:

0.3.4 may implement the minimal runtime context bridge.

### 0.3.4 Runtime Context Bridge Implementation

Package name: `0.3.4-runtime-context-bridge-implementation`

Status: planned

Type: mixed or code

Goal: Implement a minimal optional runtime context bridge from validated
generic WorldSpec-derived data.

Why this exists: After loader and bridge contracts are reviewed, the runtime
needs a small optional context path that does not break existing tick, world
time, event log, params, archive, API, or frontend behavior.

Inputs / required reading:

- 0.3.3 bridge contract and review.
- 0.3.2 loader implementation and review.
- `backend/app/core/runtime_engine.py`
- `backend/app/core/event_bus.py`
- `backend/app/world/modules/*`
- compatibility tests for runtime, events, params, and archive.

Allowed changes:

- Add `backend/app/core/runtime_context.py`.
- Add bridge module or helper.
- Allow RuntimeEngine to hold optional runtime context if existing behavior is
  preserved.
- Add compatibility tests.
- Update package review evidence.

Forbidden changes:

- Do not break `/world/step`, `/world/events`, or `/world/event-steps` legacy
  response shapes.
- Do not migrate the whole RuntimeEngine to WorldCell.
- Do not implement world generation.
- Do not implement Agent action loop.
- Do not change default runtime scaffold behavior.
- Do not introduce concrete demo world details.

Expected deliverables:

- `backend/app/core/runtime_context.py`
- focused bridge code changes, if allowed by the reviewed contract.
- `backend/app/tests/test_runtime_context_bridge.py`
- `docs/iterations/v0.3/0.3.4-runtime-context-bridge-implementation/**`
- compatibility evidence.

Expected tests / verification:

- `git diff --check`
- focused runtime context bridge pytest.
- event API compatibility pytest.
- runtime/API/params/archive compatibility checks required by contract.
- backend app tests as required by blast radius.
- concrete demo anchor sweep.

Compatibility constraints:

- Existing runtime tick and `world_time_seconds` behavior must remain
  compatible.
- API envelopes and existing response shapes must remain compatible.
- Optional `Event.refs` behavior must remain additive.
- Frontend-facing behavior must not regress.

Scope guardrails:

- Keep bridge minimal and optional.
- Do not convert WorldCell into runtime module semantics.
- Do not add generation, persistence, agent loop, memory, projection, or
  concrete world behavior.

Exit criteria:

- Runtime can hold or receive validated context through the approved bridge
  shape.
- Existing compatibility tests pass.
- Review evidence maps every compatibility surface.

Handoff to next package:

0.3.5 may prepare external fixture runner contract readiness.

### 0.3.5 External Fixture Contract Readiness

Package name: `0.3.5-external-fixture-contract-readiness`

Status: planned

Type: documentation-only or mixed

Goal: Define how external fixture runners consume WorldEngine through public
contracts without creating external repositories inside core.

Why this exists: External validation is a consumer boundary. v0.3 needs public
runner contracts and redacted evidence expectations without importing concrete
external world internals.

Inputs / required reading:

- `docs/external-fixture-boundary.md`
- `docs/validation-report-template.md`
- completed 0.3 loader and bridge package reviews.
- public API / CLI / schema / contract docs.

Allowed changes:

- Add `docs/contracts/external-fixture-runner-contract.md`.
- Create 0.3.5 package docs.
- Define public API / CLI / schema / exported-contract consumption paths.
- Define redacted validation report expectations.
- Add documentation-only examples using abstract identifiers.

Forbidden changes:

- Do not create external repositories.
- Do not add concrete fixture data.
- Do not add concrete external world names.
- Do not add UI selectors, reset API internals, or private oracle behavior.
- Do not implement product validation app.

Expected deliverables:

- `docs/contracts/external-fixture-runner-contract.md`
- `docs/iterations/v0.3/0.3.5-external-fixture-contract-readiness/**`
- review evidence.

Expected tests / verification:

- `git diff --check`
- contract heading checks.
- redaction rule checks.
- concrete demo anchor sweep.
- no backend/frontend tests unless implementation scope is explicitly added.

Compatibility constraints:

- External fixtures remain consumers.
- WorldEngine core stores only public contracts and redacted reports.
- Loader and bridge APIs must not be narrowed to one consumer.

Scope guardrails:

- Keep all examples domain-neutral.
- Do not use external validation worlds to drive internal abstractions.
- Do not add product UI or validation app logic.

Exit criteria:

- External fixture runner contract is clear enough for a separate repository to
  consume public WorldEngine behavior.
- Redacted report boundary is explicit.
- No external internals are added.

Handoff to next package:

0.3.6 may audit loader, bridge, external fixture readiness, and compatibility
evidence.

### 0.3.6 Runtime Bridge Evidence And Compatibility Audit

Package name: `0.3.6-runtime-bridge-evidence-and-compatibility-audit`

Status: planned

Type: documentation-only or mixed

Goal: Summarize loader and bridge evidence, audit compatibility, and determine
handoff readiness for v0.4 Agent-in-World minimal loop.

Why this exists: Before release-candidate work, v0.3 needs an evidence index
that distinguishes implemented behavior, documented contracts, tested
compatibility, missing evidence, and blockers.

Inputs / required reading:

- all completed v0.3 package reviews.
- loader contract and implementation evidence.
- bridge contract and implementation evidence.
- external fixture runner contract readiness docs.
- v0.2 compatibility review.

Allowed changes:

- Add `docs/iterations/v0.3/evidence-index.md`.
- Add `docs/iterations/v0.3/compatibility-audit.md`.
- Create 0.3.6 package docs.
- Record findings and handoff readiness.
- Add documentation-only status links.

Forbidden changes:

- Do not add new features.
- Do not hide P1 or P2 findings.
- Do not declare final release status while blockers remain.
- Do not patch loader or bridge behavior unless the package is explicitly
  upgraded to mixed scope with reviewed fixes.

Expected deliverables:

- `docs/iterations/v0.3/evidence-index.md`
- `docs/iterations/v0.3/compatibility-audit.md`
- `docs/iterations/v0.3/0.3.6-runtime-bridge-evidence-and-compatibility-audit/**`
- review evidence.

Expected tests / verification:

- `git diff --check`
- evidence traceability checks.
- compatibility surface grep checks.
- concrete demo anchor sweep.
- no backend/frontend tests unless implementation files are changed by a
  reviewed mixed scope.

Compatibility constraints:

- Audit must record actual current-session evidence, not assumed behavior.
- API, runtime, event, archive, params, frontend-facing, schema, fixture, and
  legacy impacts must be classified.

Scope guardrails:

- Treat gaps as findings or next-package inputs.
- Do not fill evidence gaps with unreviewed feature work.
- Do not start v0.4 implementation.

Exit criteria:

- Evidence index and compatibility audit exist.
- P1/P2/P3 findings are explicit.
- v0.4 handoff readiness is honestly classified.

Handoff to next package:

0.3.7 may prepare the release-candidate bundle if no blocking findings remain.

### 0.3.7 v0.3 Release Candidate Bundle

Package name: `0.3.7-v0.3-release-candidate-bundle`

Status: planned

Type: documentation-only

Goal: Summarize planned, implemented, tested, and not implemented v0.3 status
for human / ChatGPT final review.

Why this exists: A release candidate bundles evidence for review. It does not
declare release status or patch missing functionality.

Inputs / required reading:

- all v0.3 package reviews through 0.3.6.
- `docs/iterations/v0.3/evidence-index.md`.
- `docs/iterations/v0.3/compatibility-audit.md`.
- v0.3 release placeholder docs.
- roadmap and scope boundaries.

Allowed changes:

- Create 0.3.7 package docs.
- Add `docs/iterations/v0.3/v0.3-release-candidate-bundle.md`.
- Summarize evidence and limitations.
- Prepare final review bundle.
- Record unresolved findings.

Forbidden changes:

- Do not declare release status.
- Do not add features.
- Do not hide P1/P2 findings.
- Do not modify runtime, schema, API, frontend, tests, fixtures, or legacy
  behavior.
- Do not add concrete external-world details.

Expected deliverables:

- `docs/iterations/v0.3/0.3.7-v0.3-release-candidate-bundle/**`
- `docs/iterations/v0.3/v0.3-release-candidate-bundle.md`
- final review bundle.
- review evidence.

Expected tests / verification:

- `git diff --check`
- release-candidate wording checks.
- evidence traceability checks.
- unresolved finding checks.
- concrete demo anchor sweep.

Compatibility constraints:

- Candidate docs must reflect actual evidence only.
- P1/P2 findings must remain visible.
- Release placeholder status must remain planned / not released.

Scope guardrails:

- Bundle evidence only.
- Do not fix missing functionality in the release-candidate package.
- Keep v0.4 as handoff, not implementation.

Exit criteria:

- Release-candidate bundle is ready for human / ChatGPT review.
- No final release status is declared.
- Findings are visible and severity-classified.

Handoff to next package:

0.3.8 may perform final closeout only after release-candidate review approval.

### 0.3.8 v0.3 Final Closeout

Package name: `0.3.8-v0.3-final-closeout`

Status: planned / gated

Type: documentation-only

Goal: Perform final closeout after release-candidate review approval.

Why this exists: Final closeout is a narrow status and evidence update after
review approval. It is not a feature or cleanup package.

Inputs / required reading:

- 0.3.7 release-candidate bundle.
- human / ChatGPT final review result.
- unresolved findings list.
- roadmap and release docs.
- all v0.3 package reviews.

Allowed changes:

- Create 0.3.8 package docs.
- Add `docs/releases/v0.3.md` and `docs/releases/v0.3.zh.md` final wording if
  review approval supports it.
- Update v0.3 index and plan status.
- Record final closeout evidence.

Forbidden changes:

- Do not add features.
- Do not bypass unresolved P1/P2 findings.
- Do not claim unrun tests.
- Do not change runtime, schema, API, frontend, tests, fixtures, or legacy
  behavior.
- Do not add concrete external-world details.

Expected deliverables:

- `docs/iterations/v0.3/0.3.8-v0.3-final-closeout/**`
- `docs/releases/v0.3.md`
- `docs/releases/v0.3.zh.md`
- final closeout review evidence.

Expected tests / verification:

- `git diff --check`
- final-status wording checks.
- unresolved P1/P2 blocker checks.
- concrete demo anchor sweep.
- no backend/frontend tests unless implementation files are changed by a
  reviewed scope.

Compatibility constraints:

- Final closeout must be backed by release-candidate review evidence.
- Runtime, schema, API, frontend, tests, fixtures, and legacy behavior remain
  unchanged in the closeout package.

Scope guardrails:

- Do not use closeout to patch features.
- If P1/P2 findings remain, stop and record blockers.
- Keep v0.4 work as handoff only.

Exit criteria:

- v0.3 closeout status is updated only if review approval and evidence support
  it.
- No unresolved P1/P2 findings remain.
- v0.4 handoff is explicit.

Handoff to next package:

v0.4 may start only through its own reviewed iteration package.
