# v0.3 兼容性审计

状态：待评审

## 审计范围

本审计覆盖截至 0.3.5 已累积的 v0.3 加载器、运行时上下文桥接、外部样例契约准备
和兼容性证据。本包仅修改文档，不修补加载器或桥接行为。

## 验收问题

| 问题 | 结果 | 证据 |
|---|---|---|
| 通用 WorldSpec 数据能否被加载和校验？ | 可以，有聚焦证据 | 0.3.2 加载器实现和测试。 |
| 已验证的加载数据能否变成可选运行时上下文？ | 可以，作为惰性上下文 | 0.3.4 桥接实现和测试。 |
| 桥接是否保持 v0.1 运行时兼容？ | 对已测试表面是 | 0.3.4 运行步进、事件、参数、归档、加载器、schema smoke 测试。 |
| 外部样例是否保持在核心仓库之外？ | 是，通过契约约束 | 0.3.5 外部样例运行器契约和 review。 |
| 是否可交接给 v0.4？ | 有条件地可用于规划 | 未识别 P1/P2 阻塞；v0.4 仍必须通过自己的已评审迭代包启动。 |

## 兼容性分类

- 运行时：未改变且有证据。运行时上下文是可选且惰性的；0.3.4 测试显示既有 step 行为仍通过。
- API：未改变且有部分证据。加载器和桥接没有新增 API 暴露；事件 API 兼容测试通过。
- 事件：未改变且有证据。事件 API 和 schema 兼容测试通过，包括可选 refs 覆盖。
- 归档：未改变且有证据。archive snapshot summary 测试通过。
- 参数：未改变且有证据。world params 和 params agent 测试通过。
- Schema：未改变且有证据。WorldSpec schema smoke 测试通过；0.3.6 未修改 schema 文件。
- 前端可见行为：未触及。没有前端文件变更；本审计未运行更广的 UI smoke。
- 样例边界：已文档化。外部样例仍是公开消费者，未添加具体样例数据。
- 旧路径：未触及。Review 记录没有 `backend/worldengine/` 变更。

## 发现

- P1：未识别。
- P2：未识别。
- P3：根据 0.3.2 证据，仓库根目录直接 `pytest` 命令在当前环境不可靠；后续包的测试计划应从 `backend/` 使用后端 venv `python -m pytest`。
- P3：前端可见兼容性当前来自“无前端变更”和后端返回测试的间接证据；若发布候选评审认为该表面是发布关键项，可运行更广的 UI 或 E2E smoke。
- P3：外部样例报告和公开运行器调用可能需要在后续版本补充更严格的机器可读细节。

## 假设

- 0.3.2 和 0.3.4 的 review 证据对本审计仍足够新，因为本包不修改实现文件。
- 0.3.0 的兼容性基线仍是 v0.3 发布候选准备的正确表面列表。
- “可交接给 v0.4”表示可支撑 v0.4 规划，不表示可以在 v0.3 内启动世界内 Agent 实现。

## 发布候选验证要求

0.3.7 发布候选包应把每项发布候选声明追溯到本审计和 `evidence-index.md`。如果发布候选评审需要新鲜运行时证据，应在声明兼容性仍通过前，运行 0.3.4 记录的后端 venv pytest 命令。

## 最终判断

本审计已可评审。从已记录的 v0.3 证据看，没有识别 P1 或 P2 兼容性阻塞。该文档包评审后，v0.3 可以进入发布候选包准备。
