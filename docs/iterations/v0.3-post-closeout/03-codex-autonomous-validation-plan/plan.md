# Plan

## Ordered Execution Steps

1. Read parent campaign docs.
2. Read v0.3 release and evidence docs.
3. Read loader, bridge, RuntimeEngine, WorldCell, Event, and focused tests.
4. Define reviewer inputs.
5. Define reviewer commands.
6. Define unsupported-claim checks.
7. Define loader, bridge, RuntimeEngine, Event.refs, and API/schema/runtime
   compatibility checks.
8. Define concrete demo-world regression check.
9. Define blocker and finding classification.
10. Hand off to `04-codex-autonomous-validation-execution`.

## Phase Boundaries

This package plans the autonomous review. It does not execute it.

## Stop Conditions

Stop if:

- required reviewer inputs are missing.
- the plan depends on implementer summaries.
- the plan authorizes code changes.
- the plan marks autonomous validation as already successful.
- the plan changes v0.3 release status.

## Review Update Step

Update `review.md` with changed files, commands not run, no-test rationale,
scope review, compatibility review, and final assessment.
