# v0.2 Plan

Status: planned

英文版本：`v0.2-plan.md`。

## Version Name

Recursive World Foundation。

## Objective

建立 documentation governance、north star、recursive world schema/spec language、additive event
contract、generic schema smoke validation、external fixture boundary 和 legacy boundary。

## Non-goals

- 不迁移 RuntimeEngine 到 WorldCell。
- 不实现 full world generation。
- 不实现 Agent pseudo-self continuity。
- 不实现 demo-specific runtime。
- 不创建 game repository。
- 不增加 vector memory。
- 不增加 multi-agent society simulation。

## Execution Packages

### 0.2.1 Project North Star

Type: docs-only。

Deliverables：

- `AGENTS.md`
- `docs/project-north-star.md`
- `docs/product-model.md`
- `docs/scope-boundaries.md`
- `docs/roadmap.md`
- `docs/glossary.md`
- iteration governance 和 templates。

### 0.2.2 Recursive World Contract

Type: schema + tests。

Status: review complete。

Deliverables：

- `backend/app/schemas/entity.py`
- `backend/app/schemas/world_cell.py`
- `backend/app/tests/test_world_cell_schema.py`

### 0.2.3 Event Contract Extension

Type: backward-compatible schema extension。

Status: review complete。

Deliverables：

- extend `backend/app/schemas/event.py`
- add compatibility tests。

### 0.2.4 WorldSpec Reference Fixture

Type: fixture + validation tests。

Status: historical artifact。

Historical note：

- 该 package 保留为 prior fixture work 的 review evidence。
- 其 concrete fixture direction 已被 0.2.5 supersede，不能再作为 future fixture 或
  roadmap direction。

### 0.2.5 Core Boundary Cleanup and Roadmap Reset

Type: mixed。

Deliverables：

- active documentation cleanup。
- `docs/external-fixture-boundary.md`
- `docs/validation-report-template.md`
- 删除 concrete external-world fixture data。
- generic WorldSpec schema smoke tests。
- 本 package 的 `review.md` closeout evidence。

### 0.2.6 Generic Recursive World Foundation Closeout

Type: release docs。

Deliverables：

- `docs/releases/v0.2.md`
- generic foundation test/evidence summary。

## Evidence Rule

每个 package 必须在 `review.md` 中记录 commands run、test results、compatibility review、scope review
和 unresolved findings。Docs-only packages 可以跳过 code tests，但必须说明原因。
