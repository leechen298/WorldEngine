# v0.5 Plan

状态：planned / ready for review

## Version Name

Memory And Self-Continuity Substrate

## Objective

把 WorldEngine 从 v0.4 request-driven Agent-in-World loop 推进到第一层可检查的
memory 与 self-continuity substrate，同时保持 v0.4 loop compatibility，并把
generation、external validation readiness 和 projection application readiness 排除在范围外。

## Version Boundaries

v0.5 可以定义或实现：

- 带 provenance、bounded lifetime semantics 和 inspectable sources 的 working memory
  records。
- 连接 events、ticks、world time 和 action outcomes 的 episodic memory records。
- relationship state schema semantics。
- self-summary schema semantics。
- reflection record schema semantics。
- personality drift signal schema semantics。
- 只有在已评审 child package 授权后，才实现第一层 generic in-memory substrate。
- 只有在已评审 child package 授权后，才把 bounded read-only memory context 接入
  Agent Loop。
- 针对被触及 surfaces 的 focused backend tests 和 compatibility evidence。
- evidence audit、release-candidate 和 final closeout docs。

v0.5 不得实现：

- world generation；该范围属于 v0.6。
- external validation runner readiness 或 report automation；该范围属于 v0.7。
- projection application readiness；该范围属于 v0.8。
- concrete world names、maps、characters、locations、resources、story rules、
  seed data、UI-specific app behavior 或 private validation oracle details。
- frontend product behavior，除非后续已评审 v0.5 child 明确授权。
- durable persistence 或 migrations，除非后续已评审 v0.5 child 明确授权。
- `backend/worldengine/` 下的新 runtime features。

## Capability Boundary Split

| Capability | v0.5 docs contract | First implementation package |
| --- | --- | --- |
| Working memory | 定义 concept、provenance、bounded read/write semantics | 是：additive schema + generic in-memory substrate |
| Episodic memory | 定义 event-linked episode semantics | 是：additive schema + generic in-memory substrate |
| Relationship state | 只定义 schema semantics | 暂不实现 behavior |
| Self-summary | 只定义 schema semantics | 暂不实现 summarization behavior |
| Reflection records | 只定义 schema semantics | 暂不实现 automatic reflection |
| Personality drift signals | 只定义 schema semantics | 暂不实现 action modifier |

## Public Interface Intent

`0.5.0` 不新增 public runtime APIs 或 implementation files。

未来计划中的 implementation interfaces 可以包括：

- planned schema module：`backend/app/schemas/agent_memory.py`。
- planned service/store module：`backend/app/agent/memory.py` 或等价的已批准路径。
- planned tests：`backend/app/tests/test_agent_memory_*.py`。
- 只有在 `0.5.3-memory-context-loop-integration` 完成评审和授权后，才允许对
  `PerceptionFrame` 做 bounded additive extensions。

## Required Compatibility Baseline

任何 implementation-bearing v0.5 child 在改变 runtime、schema、API、event、
archive、params、frontend-facing、test、fixture、migration 或 legacy behavior 前，
都必须为 touched surface 提供 current-session evidence，并为 adjacent compatibility
surfaces 提供 focused regression evidence。

Compatibility-sensitive surfaces 包括：

- `PerceptionFrame`。
- `ActionIntent`。
- `ActionResult`。
- request-scoped `LoopStep`。
- `POST /world/agent/loop/step`。
- `/world/agent/params/propose-and-apply`。
- runtime tick 和 world time behavior。
- API envelope 和 error shape。
- `/runtime/state`、`/runtime/step`、`/world/events`、`/world/event-steps`。
- world params 和 params apply behavior。
- archive behavior。
- `Event.refs` optional serialization。

除非 active child contract 明确允许 breaking change，否则 schema changes 必须是 additive。

## Codex `/goal` And Subagent Model

`完成 v0.5` 使用 `GOAL_RUNNER.md`、`CURRENT_STATE.md` 和 `CAMPAIGN_PLAN.md`。
Implementation-bearing children 在 closeout 前必须完成 documentation/contract
evaluator、implementation-scope evaluator、code-review evaluator、
validation-evidence evaluator 和 closeout consistency review。

会改变 goal routing、evidence rules、package sequencing、automation-consumption
contracts、release status、validation templates 或 mirror obligations 的
documentation-only children，必须使用 read-only documentation evaluator。

## Planned Packages

### 0.5.0 v0.5 Planning And Continuity Boundary Baseline

Package name：`0.5.0-v0.5-planning-and-continuity-boundary-baseline`

状态：planned / ready for review

类型：documentation-only

目标：创建 v0.5 documentation root、goal-campaign controls、version plan、
memory/self-continuity boundary、compatibility baseline 和 v0.4 handoff mapping，
且不修改 implementation files。

存在原因：先建立 deterministic docs，并在 v0.5 product boundaries 接受评审期间保持
implementation authorization closed。

Inputs / required reading：

- `AGENTS.md`
- `docs/project-north-star.md`
- `docs/product-model.md`
- `docs/scope-boundaries.md`
- `docs/roadmap.md`
- `docs/iterations/README.md`
- `docs/iterations/AGENTS.md`
- `docs/iterations/v0.4/CURRENT_STATE.md`
- `docs/iterations/v0.4/0.4.7-v0.4-final-closeout/final-closeout.md`
- `docs/iterations/v0.4/review.md`
- `docs/iterations/v0.4-post-closeout/CURRENT_STATE.md`
- `docs/testing/results/2026-05-31-v0.4-overall-product-capability-validation.md`
- active package files under
  `docs/iterations/v0.5/0.5.0-v0.5-planning-and-continuity-boundary-baseline/`

Allowed changes：

- 创建 `docs/iterations/v0.5/**` parent 和 child documentation。
- 定义 goal entry `完成 v0.5`。
- 定义 subagent/evaluator checkpoints 与 package sequence。
- 将 v0.4 final closeout 和 post-closeout clean pass evidence 只作为 handoff context
  记录。
- 定义哪些 v0.5 capabilities 是 contract-only，哪些是首个 implementation candidates。

Forbidden changes：

- 本 documentation-only package 不得修改 runtime、schema、API、frontend、
  backend test、fixture、migration、external repository、generated result 或 legacy
  implementation files。
- 本 package 不得实现任何 memory、self-continuity、loop integration、API、UI、
  migration 或 persistence behavior。
- 不添加 world generation、external validation readiness、projection app readiness、
  concrete world content、private validation details 或 application-specific backend
  logic。
- 不在 `backend/worldengine/` 下新增 runtime feature。

Expected deliverables：

- 完整 v0.5 parent campaign docs 和中文镜像。
- 完整 `0.5.0` package docs 和中文镜像。
- Documentation-only verification，以及不运行 code tests 的理由。
- Review 记录 changed files、commands、compatibility review、scope review、
  subagent/evaluator findings 和 P1/P2/P3 findings。

Expected tests / verification：

- `git status --short --branch`
- `git diff --check`
- 检查必需 v0.5 parent 和 child docs 及 mirrors 是否存在
- changed-file scope guard，确认只修改 `docs/iterations/v0.5/**`
- 将 backend/frontend/API/E2E/runtime tests 记录为未运行，因为本 package 是
  documentation-only。

Compatibility constraints：

- v0.4 Agent Loop schemas 和 API 必须保持兼容，除非后续已评审 child 明确以 additive
  方式改变。
- Runtime tick 和 world time behavior 必须保持兼容。
- 现有 API envelope 和 error shape 必须保持兼容。
- Event routes、params behavior、archive behavior 和 optional `Event.refs`
  serialization 是 compatibility-sensitive。
- Historical evidence 不得提升为当前 v0.5 pass evidence。

Scope guardrails：

- 缺少必需 evaluator checkpoint 时停止。
- 出现 P1 或 unresolved P2 findings 时停止。
- 需要的 file classes 未被 active contract 授权时，停止并记录 blocker。
- 不把 historical evidence 当作 current-session pass evidence。

Exit criteria：所有必需 v0.5 parent 和 `0.5.0` docs 存在，mirrors 存在，
documentation checks 通过，review 记录无 unresolved P1/P2。

Handoff to next package：`0.5.1-memory-self-continuity-contracts` 接收已评审的
boundary baseline。`0.5.0` 不启动 runtime implementation。

### 0.5.1 Memory Self-Continuity Contracts

Package name：`0.5.1-memory-self-continuity-contracts`

状态：planned

类型：documentation-only

目标：在 implementation 前定义 public memory/self-continuity concepts、schema
semantics、provenance rules、compatibility requirements 和 authorization criteria。

存在原因：六个 v0.5 capabilities 必须先作为 contracts 评审，再实现 schema、service、
API 或 loop behavior。

Allowed changes：

- 创建或更新 `docs/iterations/v0.5/` 下的 package docs。
- 定义 working memory、episodic memory、relationship state、self-summary、
  reflection records 和 personality drift signals 的 public concepts。
- 定义 planned schema semantics 和 evidence requirements。

Forbidden changes：

- 本 package 不实现 schemas、stores、services、APIs、frontend、fixtures、migrations
  或 tests。
- 不让 memory 影响 action choice 或 loop output。
- 不添加 concrete world content 或 external validation internals。

Expected deliverables：

- 完整 package docs 和中文镜像。
- Contract-only review evidence。
- `0.5.2` 的明确 authorization criteria。

Expected tests / verification：

- Documentation checks、mirror checks 和 scope guard。
- 不运行 code tests，并记录 docs-only rationale。

Compatibility constraints：

- 保持 v0.4 loop 和 API compatibility。
- Planned schema changes 保持 additive。

Scope guardrails：

- 在 contract review 前尝试实现 behavior 时停止。

Exit criteria：Review 记录无 unresolved P1/P2，并将稳定 contract 交给 `0.5.2`。

Handoff to next package：`0.5.2-working-and-episodic-memory-substrate`。

### 0.5.2 Working And Episodic Memory Substrate

Package name：`0.5.2-working-and-episodic-memory-substrate`

状态：planned

类型：mixed or code

目标：实现第一层 additive generic working-memory 和 episodic-memory schemas、
in-memory substrate 与 focused backend tests。

存在原因：Working 和 episodic memory 是最小且安全的 implementation slice。它们提供
evidence-bearing records，但不修改 action semantics、relationship behavior、
summary generation、reflection 或 personality drift behavior。

Allowed changes：

- 在 `backend/app/schemas/agent_memory.py` 或已批准等价路径下添加 additive schemas。
- 在 `backend/app/agent/` 或已批准等价路径下添加 generic in-memory memory substrate
  service/store。
- 在 `backend/app/tests/test_agent_memory_*.py` 下添加 focused backend tests。
- 更新 package docs 和 review evidence。

Forbidden changes：

- 除非本 package contract 扩展并在 implementation 前完成评审，否则不添加 public
  runtime APIs。
- 不把 memory 接入 `POST /world/agent/loop/step`。
- 不实现 relationship behavior、self-summary generation、automatic reflection、
  personality drift action modifiers、durable persistence、frontend behavior、
  migrations、concrete world content 或 external validation internals。
- 不修改 `backend/worldengine/`。

Expected deliverables：

- Additive schema 和 service implementation。
- Focused backend tests 和 adjacent compatibility tests。
- 包含必需 subagent/evaluator checkpoints 的 review。

Expected tests / verification：

- Focused backend memory tests。
- Adjacent v0.4 loop/API compatibility tests。
- 如果 implementation 触及 shared schemas 或 app factory state，运行 full backend
  regression。
- Documentation checks 和 scope guard。

Compatibility constraints：

- Existing v0.4 loop responses 保持兼容，除非 later package 明确评审了 additive field。
- Existing params、events、archive 和 API envelope behavior 保持不变。

Scope guardrails：

- 如果需要 durable persistence、public API 或未评审的 loop integration，则停止。

Exit criteria：Implemented substrate 通过 focused 和 required regression checks，
且没有 unresolved P1/P2。

Handoff to next package：`0.5.3-memory-context-loop-integration`。

### 0.5.3 Memory Context Loop Integration

Package name：`0.5.3-memory-context-loop-integration`

状态：planned

类型：mixed or code

目标：把 bounded read-only memory context 接入 Agent Loop perception path，但不改变
action semantics。

存在原因：只有在 substrate 存在后，loop 才能通过 bounded、inspectable、additive
context 消费 memory。

Allowed changes：

- 在已批准 agent/perception path 中添加 read-only memory context assembly。
- 只有经过评审后，才添加 additive perception schema fields。
- 添加 focused service/API compatibility tests。

Forbidden changes：

- 不改变 accepted action types 或 params patch semantics。
- 不让 personality drift、relationship state、reflection 或 self-summary 改变
  action results。
- 不添加 frontend behavior、migrations、concrete worlds 或 external validation
  internals。

Expected deliverables：

- Bounded read-only memory context integration。
- Focused tests，证明 old requests remain compatible 且 memory context bounded。
- 包含必需 evaluator checkpoints 的 review evidence。

Expected tests / verification：

- Focused perception/loop tests。
- `POST /world/agent/loop/step` API compatibility tests。
- 按需运行 adjacent params/event/runtime tests。

Compatibility constraints：

- Existing loop callers 必须继续可用。
- Existing API envelope 和 error behavior 保持不变。

Scope guardrails：

- 如果 integration 需要改变 action semantics 或 persistent storage，则停止。

Exit criteria：Integration 是 additive、bounded、tested 且 reviewed。

Handoff to next package：
`0.5.4-reflection-relationship-and-drift-contract-followup`。

### 0.5.4 Reflection Relationship And Drift Contract Follow-up

Package name：`0.5.4-reflection-relationship-and-drift-contract-followup`

状态：planned

类型：documentation-only or mixed

目标：在任何 behavior 影响 action 前，细化 relationship state、self-summary、
reflection record 和 personality drift signal contracts。

存在原因：这些概念比 working/episodic memory 风险更高，因为它们可能意味着 agent
identity changes、summarization behavior 和 action modifiers。

Allowed changes：

- 更新 v0.5 docs 和 package contracts。
- 只有当 package 在 implementation 前明确作为 mixed 或 code 完成评审时，才添加
  schema-only work。

Forbidden changes：

- 不添加 automatic reflection、self-summary generation、personality drift action
  modification、relationship behavior、frontend behavior 或 external validation
  internals，除非有后续 implementation package。

Expected deliverables：

- 已评审 concept 和 schema semantics。
- 明确决定 implementation 是 deferred 还是拆到后续 child packages。

Expected tests / verification：

- Docs-only scope 的 documentation checks，或当 package 经评审变为 mixed/code 后执行
  focused schema tests。

Compatibility constraints：

- 未评审 change 不得影响 loop action output。

Scope guardrails：

- Behavior design 超出 approved contract 时停止。

Exit criteria：无 unresolved P1/P2，并清楚 handoff 到 audit 或后续 implementation
packages。

Handoff to next package：`0.5.5-v0.5-evidence-and-compatibility-audit`。

### 0.5.5 v0.5 Evidence And Compatibility Audit

Package name：`0.5.5-v0.5-evidence-and-compatibility-audit`

状态：planned

类型：documentation-only

目标：审计 v0.5 implementation evidence、compatibility surfaces、unresolved findings
和 release-candidate review handoff readiness。

存在原因：v0.5 在 evidence、compatibility 和 scope 同步前，不应进入 release-candidate
review。

Allowed changes：

- `docs/iterations/v0.5/**` 下的 documentation/evidence updates。
- Compatibility audit 和 evidence index。

Forbidden changes：

- 不实现新的 runtime、schema、API、frontend、test、fixture、migration 或 external
  repository behavior。

Expected deliverables：

- Evidence index。
- Compatibility audit。
- Review evidence 和 unresolved finding classification。

Expected tests / verification：

- Documentation checks。
- Scope guard。
- 只有当 audit contract 要求 fresh implementation evidence 时，才重跑 focused/broad
  commands。

Compatibility constraints：

- Audit 必须反映真实 current git state 和 command evidence。

Scope guardrails：

- 遇到 stale status、missing evidence 或 unresolved P1/P2 时停止。

Exit criteria：Audit review complete 且无 unresolved P1/P2。

Handoff to next package：`0.5.6-v0.5-release-candidate-bundle`。

### 0.5.6 v0.5 Release Candidate Bundle

Package name：`0.5.6-v0.5-release-candidate-bundle`

状态：planned

类型：documentation-only

目标：基于已评审 implementation 和 audit evidence 准备 v0.5 release-candidate bundle，
但不声明 final release。

存在原因：Release candidate review 必须在 final closeout 前独立可检查。

Allowed changes：

- Release-candidate documentation 和 review evidence。

Forbidden changes：

- 不声明 final release。
- 不修改 implementation files。

Expected deliverables：

- Release-candidate bundle 和中文镜像。
- Reviewer checklist 和 findings。

Expected tests / verification：

- Documentation checks 和 scope guard。
- 将 implementation commands 记录为 historical，或在 package 要求 fresh evidence 时
  重新运行。

Compatibility constraints：

- Release candidate 不得夸大 capability 或 validation status。

Scope guardrails：

- Evidence stale 或 missing 时停止。

Exit criteria：Release candidate 准备好进入 final review，且无 unresolved P1/P2。

Handoff to next package：`0.5.7-v0.5-final-closeout`。

### 0.5.7 v0.5 Final Closeout

Package name：`0.5.7-v0.5-final-closeout`

状态：planned

类型：documentation-only

目标：只有在 release-candidate review approval、evidence consistency checks 和
unresolved finding classification 完成后，才标记 v0.5 final / closeout complete。

存在原因：Final closeout 必须引用 evidence，并避免未验证的 runtime、API、E2E、build、
Agent smoke、autonomous validation 或 release claims。

Allowed changes：

- Package 授权的 final closeout docs 和 status surfaces。

Forbidden changes：

- 不修改 implementation files。
- 不声明 v0.6 generation、v0.7 external validation readiness、v0.8 projection
  readiness、concrete product readiness 或 unrun validation。

Expected deliverables：

- Final closeout record。
- Parent status updates。
- 包含 commands、scope、compatibility 和 unresolved findings 的 review evidence。

Expected tests / verification：

- Documentation checks 和 scope guard。
- 只有 final closeout contract 要求时，才运行 fresh implementation/regression evidence。

Compatibility constraints：

- 如果触及 README、roadmap/status surfaces、current state 和 review evidence，final
  status 必须一致。

Scope guardrails：

- 出现 stale status drift、missing evidence 或 unresolved P1/P2 时停止。

Exit criteria：只有 final review approval 和 evidence consistency checks 通过后，
v0.5 才可标记为 `final / closeout complete`。

Handoff to next version：v0.6 world generation v1 只能从自己的 reviewed iteration
package 启动。

