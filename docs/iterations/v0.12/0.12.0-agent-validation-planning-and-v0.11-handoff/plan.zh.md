# Plan

英文源文件：`plan.md`。

1. 读取 parent v0.12 current state、goal runner、campaign plan 和 v0.12 plan。
2. 读取 v0.11 closeout current state、review 和 0.11.5 review。
3. 创建本 package document set 和镜像。
4. 运行 `test-plan.md` 中的 documentation checks。
5. 请求 documentation evaluator review。
6. 如果 PASS，同步 parent v0.12 route 到 `0.12.1`。
7. 在 `0.12.1` 拥有自己的 reviewed package docs 前，保持 implementation
   authorization 关闭。
