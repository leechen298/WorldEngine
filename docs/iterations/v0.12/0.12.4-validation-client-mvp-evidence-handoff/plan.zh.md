# Plan

英文原文：`plan.md`。

1. 读取 parent v0.12 current state、goal runner、campaign plan、v0.12 plan 和 `0.12.3` closeout。
2. 读取现有 manifest、public surface、Agent、memory 和 inspection evidence contracts。
3. 起草 package docs、artifact contract 和 handoff prompt。
4. 运行 documentation completeness、authorization、redaction-boundary 和 whitespace checks。
5. 请求 documentation evaluator review。
6. 在本 package scope 内修复 P1/P2 findings。
7. 如果 documentation review 通过，在不做 code implementation 的情况下关闭这个 documentation/contract package，除非 reviewed contract 明确授权 focused schema/checker support change。
8. 更新 parent route 到 `0.12.5-full-lifecycle-checker-and-autonomous-validation-documentation-package-needed`。
