# Plan

Chinese mirror: `plan.zh.md`.

1. Read parent v0.12 current state, goal runner, campaign plan, v0.12 plan,
   `0.12.0` handoff, and `0.12.1` closeout.
2. Read existing Agent memory schemas/store, session Agent runtime loop,
   session routes, manifest route, and related tests.
3. Complete this package document set and run documentation checks.
4. Request documentation / contract evaluator review.
5. If PASS, record `implementation_authorized: yes` for this package only.
6. Add focused tests for public memory read, working summary creation, rest
   consolidation, redaction, no personality/skill mutation, evidence refs, and
   manifest discovery.
7. Implement the smallest additive public memory/consolidation path.
8. Run focused backend verification from `test-plan.md`.
9. Request implementation-scope evaluator review.
10. Repair P1/P2 findings inside this package scope.
11. If verification and evaluator review pass, update package and parent v0.12
    route to the active `0.12.3-narrative-and-diagnostic-inspection-surfaces`
    route recorded in parent `CURRENT_STATE.md`.
