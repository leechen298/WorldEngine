# Plan

英文版本：`plan.md`。

1. 读取 parent v0.11 current state、goal runner、campaign plan 和 v0.11 plan。
2. 读取 v0.10 closeout current state、review 和 0.10.6 review。
3. 创建本 package document set 和镜像。
4. 运行 `test-plan.md` 中的 documentation checks。
5. 请求 documentation evaluator review。
6. 如果 PASS，同步 parent v0.11 route 到 `0.11.1`。
7. `0.11.1` 自己的 package docs 通过 review 前，implementation authorization 保持关闭。
