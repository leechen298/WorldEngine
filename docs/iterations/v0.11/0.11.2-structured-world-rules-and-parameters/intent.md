# Intent

Chinese mirror: `intent.zh.md`.

The MVP world cannot evolve legally if its rules and parameters are only prose
or hidden model output. This package makes rule/parameter structure session
visible and validator-readable.

The goal is not to apply events. The goal is to ensure a session can carry a
validated public rule set that later packages can cite when accepting,
rejecting, and applying events.
