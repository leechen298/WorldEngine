# Intent

Status: package complete / passed current campaign

## Problem / Purpose

The autonomous validation plan needs an execution package that keeps the
independent review separate from verification of that review's quality.

## Why Now

The current campaign has accepted `03-codex-autonomous-validation-plan`; it now
needs the independent review result before the final validation bundle can
close out.

## Relationship To Roadmap

This review validates v0.2 evidence for future planning confidence. It does
not implement Agent-in-World behavior.

## Non-Goals

- Do not execute autonomous validation outside this package.
- Do not modify code.
- Do not mark unrun tests as successful.

## Expected Handoff

The completed independent review feeds the final validation bundle.
