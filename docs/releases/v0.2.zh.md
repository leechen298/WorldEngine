# WorldEngine v0.2 Release Draft

状态：`release candidate / not released / not final`

英文版本：`v0.2.md`

## 版本摘要

v0.2 是 Recursive World Foundation release candidate。它不是 final release
status。

本文件是 release draft，不是 release announcement。final closeout 只会在
`0.2.12-v0.2-final-closeout` 之后进行，并且必须先通过 0.2.11
release-candidate bundle 的 human / ChatGPT final review。

## Candidate Foundation Pieces

v0.2 已有 review evidence 支撑：

- project north star 和 documentation governance。
- EntityRef、WorldCell 和 WorldSpec schema/spec concepts。
- EventRef 和 optional Event.refs additive event structure。
- generic WorldSpec schema smoke validation。
- external fixture 和 validation boundaries。
- redacted validation report template。
- active `backend/app/` 和 legacy `backend/worldengine/` boundary documentation。
- iterative automation workflow 和 final-review handoff。
- evidence index、boundary audit、compatibility review 和 release-candidate
  bundle。

## Package Status

- 0.2.1 through 0.2.3：review complete。
- 0.2.4：historical artifact，已被 0.2.5 superseded。
- 0.2.5 through 0.2.10：review complete。
- 0.2.11：release-candidate bundle complete and final review requested。
- 0.2.12：approval 后 planned final closeout。

## Candidate Capability Boundary

v0.2 可以 claim evidence for：

- 描述 WorldEngine 的 north star 和 product boundaries。
- 定义 minimal recursive WorldCell abstraction。
- 定义 generated/loadable world 的 WorldSpec shape。
- 扩展 Event contract 并保持 backward compatibility。
- validate generic WorldSpec schema smoke behavior。
- 定义 external fixture 和 validation boundaries。
- 明确 `backend/app/` 是 active path，`backend/worldengine/` 是 legacy path。
- 提供 release-candidate evidence 供 review。

v0.2 不声明它可以：

- 在 RuntimeEngine 中运行 WorldCell。
- 把 WorldSpec 加载到 runtime。
- 从 prompt 生成 worlds。
- 形成 agent pseudo-self。
- 运行 demo-specific application behavior。
- 提供 product client。

## 已知限制

- WorldSpec loading 和 runtime bridge behavior 未实现。
- RuntimeEngine 保持 v0.1 scaffold，未迁移到 WorldCell。
- Agent-in-world loop、memory、pseudo-self continuity、generation、projection
  API、product UI、external fixture repository 和 external validation repository
  都是 future scope。
- 当前 0.2.11 verification 是 documentation-only。它没有重跑 backend、
  frontend、API smoke、E2E、Agent smoke、runtime、schema execution、fixture 或
  migration tests。
- `v0.2-P3-003` 保持 open 作为 v0.3 handoff，要求 future bridge behavior
  changes 前提供 current-session compatibility evidence。

## 测试与证据摘要

Evidence 汇总在：

- `docs/iterations/v0.2/evidence-index.md`
- `docs/iterations/v0.2/boundary-audit.md`
- `docs/iterations/v0.2/compatibility-review.md`
- `docs/iterations/v0.2/v0.2-release-candidate-bundle.md`
- `docs/iterations/v0.2/0.2.11-v0.2-release-candidate-bundle/final-review-bundle.md`

Historical code-package reviews 记录了 focused schema、event、generic smoke 和
backend checks。0.2.11 只记录 release-candidate documentation checks、status
checks、evidence traceability checks 和 scope guard checks。

## 建议下一步

Review 0.2.11 release-candidate bundle。若 human / ChatGPT review 批准，执行
`0.2.12-v0.2-final-closeout`。不要在 v0.2 closeout 完成且独立 v0.3 package
通过 review 前启动 v0.3 loader 或 runtime bridge implementation。
