# WorldEngine v0.1 Release Closeout

Status: closeout documented

## Version Summary

v0.1 is the current verified scaffold baseline for WorldEngine.

It establishes:

- monorepo structure.
- FastAPI backend.
- Vue 3 + TypeScript dashboard.
- root development workflow.
- runtime tick and world-time primitives.
- API response envelope.
- in-memory event log.
- event timeline APIs.
- world module tree execution.
- world params API and dashboard surface.
- static and dry-run params validation.
- archive snapshots and summaries.
- LLM-style params agent propose-and-apply service path.

v0.1 is intentionally not the recursive world foundation. That belongs to v0.2.

## Current Capability Boundary

Current v0.1 can:

- start backend and frontend development services through root Make targets.
- expose `GET /health`.
- expose `GET /runtime/state` and `POST /runtime/step`.
- advance runtime ticks and world time.
- append `tick.advanced` and module events.
- expose `GET /world/events` with filters and cursor pagination.
- expose `GET /world/event-steps` grouped by tick.
- execute the default world module tree with heartbeat and counter examples.
- expose `GET /world/params` and `POST /world/params/apply`.
- validate world param patches before applying them.
- dry-run world param patches without polluting the active module state.
- support configurable dry-run validation policy through env and world
  overrides.
- emit `params.applied` events and include current params in runtime event
  payloads.
- create in-memory snapshots at configured tick intervals.
- create in-memory summaries at configured tick intervals.
- expose snapshot and summary list/detail APIs.
- run an LLM-style params agent service that can propose and apply validated
  world parameter patches.
- render frontend controls for runtime stepping, timeline/event inspection,
  world params, patch details, and agent params interactions.

Current v0.1 cannot:

- represent recursive WorldCell structures.
- load a structured WorldSpec.
- generate worlds.
- run agents through a perception/action/memory loop.
- model agent pseudo-self continuity.
- run a village reference world.
- provide a game surface.
- persist full production-grade world state.
- provide a stable external API contract for recursive world consumers.

## Known Limitations

- World and agent schemas are still lightweight placeholders.
- Runtime behavior is not yet driven by WorldSpec.
- Event structure is minimal.
- Persistence is in-memory or placeholder-level.
- `backend/worldengine/` remains legacy code outside the active path.
- Frontend production build currently emits a non-blocking Vite chunk-size
  warning for a 1,513.97 kB JS bundle.
- API curl smoke is intentionally outside the final v0.1 Agent/Codex testing
  plan.
- No full Agent autonomous scenario suite, scorecard checker, persistence
  restart test, recursive world test, WorldSpec/WorldCell test, or
  agent memory/pseudo-self test exists in v0.1.

## Test and Evidence Summary

Recorded v0.1 closeout evidence is mapped in
`docs/testing/v0.1-test-map.md`.

Primary evidence references:

- `docs/testing/results/2026-05-23-v0.1-closeout.md`
- `docs/testing/results/2026-05-23-v0.1-e2e-agent-acceptance.md`
- `docs/testing/results/2026-05-24-v0.1.8-params-flow-live-smoke.md`
- `test-results/agent-smoke/latest/`

Current implementation documentation:

- `docs/current-implementation.md`
- `docs/backend-implementation.md`
- `docs/frontend-implementation.md`
- `docs/api-reference-v0.1.md`
- `docs/testing/v0.1-test-map.md`

Recorded results:

- `make help`: passed and printed root workflow commands.
- `make check-backend`: passed.
- `make check-frontend`: passed.
- Backend pytest: `63 passed in 2.93s`.
- Frontend vitest: `5 passed (5)` test files, `24 passed (24)` tests.
- Frontend build: passed with the documented chunk-size warning.
- `make test-e2e`: `6 passed`.
- Focused frontend tests for current-code coverage: `28 passed`.
- Backend runtime/legacy diff guard: passed with no `backend/app/` or
  `backend/worldengine/` runtime change.
- Live Agent smoke `dashboard-params-flow`: 0.1.8 deterministic evidence is
  preserved by `docs/testing/results/2026-05-24-v0.1.8-params-flow-live-smoke.md`
  and commit `c6da552`.
- Live Agent smoke `dashboard-invalid-param`: current deterministic evidence is
  recorded under `test-results/agent-smoke/latest/`.

These are recorded closeout results. This release-document sync does not rerun
backend, frontend, E2E, or Agent smoke tests.

## Release Candidate Checklist

- [x] v0.1 capability boundary documented.
- [x] Root README updated with current status and capability summary.
- [x] Backend test suite passed.
- [x] Frontend unit test suite passed.
- [x] Frontend production build passed.
- [x] Final E2E evidence recorded.
- [x] Live Agent smoke evidence recorded for current v0.1 scope.
- [x] Final test map points to the recorded evidence.
- [x] Known limitations documented.
- [x] v0.2 boundary kept separate from v0.1 capability.
- [x] User review complete.

## Recommended Next Work

v0.2 Recursive World Foundation:

- documentation governance.
- WorldCell and WorldSpec schema/spec foundation.
- additive event contract extension.
- reference WorldSpec fixture.
- legacy boundary cleanup.

Decision: v0.2 may start after this closeout, but only as Recursive World
Foundation. It must not reinterpret v0.1 as an already recursive runtime.
